# Xike Lab 可视化面板项目交接

更新时间：2026-05-26 10:43 CST
项目路径：`/Users/hxx/Desktop/00_项目/05_Claude可视化面板`
当前分支：`codex/paperclip-local-governance`
当前状态：有未提交增量；Paperclip 本地治理能力未被删除；新增 Agent/Skill Registry、Activity 追溯、Agent Run 会话化、工程上下文分派、Codebase Index、AST 优先 Symbol Graph、本地权限治理与侧边栏导航优化均已提交到当前分支。最新续推进：P0 Codebase Index 二期增加 per-file evidence cache、SQLite FTS/BM25、本地 hash-vector、TS/TSX/JSX AST、import/export 绑定、TS 类型级引用、TS assertion/satisfies/instantiation、method/member 语义引用、本地 SQLite snapshot 持久化、citation chain 与 code question UI，rebuild 时基于 mtime/size/hash 复用未变化文件并在 status 暴露 `cacheStats`，query 合并 `fts5/bm25/sqlite-fts` reason、`bm25Rank`、`semanticScore`、`persistentSummary` 与 `citationSummary`；`POST /api/codebase-index/question` 会把 query/citation 结果压成本地 deterministic code answer，返回 `answer`、`answerLines`、`citations`、coverage 和 limitations，Codebase Center 新增 `Answer` 与 `Local Code Answer` 区，不调用模型、不上传代码、不做黑盒总结；最新又把 `codebaseQuestionAnswer` 注入 Dispatch Preview、`/api/agent-registry/classify` prompt preview、`AgentRunStore.createIdeaRun()` details/decision/summary/intake archive 与 Run detail，Codebase Answer 不再只停在查询面板，而能作为后续 Agent Run 本地证据。P0 Agent Run 会话化补上 governance lineage，`/api/agent-runs/:id` 直接返回 activityEvents / governanceLineage / archives，Runs tab 支持治理筛选并展示 approval / delegation / budget / autopilot 链路，`/api/agent-runs/:id/replay-plan` 可记录安全 replay plan，`/api/agent-runs/:id/replay-result` 可归档 replay result，`/api/agent-runs/:id/archive` 可记录执行后 archive artifact 并展示 Execution Archive，`/api/governance/summary` 现在返回 counts / blockers / nextActions / sections 并聚合审批、预算、委派、Autopilot、带治理链的 Agent Runs 和最近治理 Activity，侧边栏 `治理` 入口打开 Governance Center 统一视图；最新又补 `AgentRunStore.getSessionSnapshot()`、`GET /api/agent-runs/session/:sessionId`、详情 `includeSession=true` 与 Runs tab `sessionId` 筛选 / Session Timeline，把同一 session 下的 runs、messages、tool results、archives、Activity、status/source/profile counts、session-level blockers 和 next actions 聚合到 run 详情；`/api/agent-runs/idea` 可从 Dispatch Preview 创建 `sourceType=idea_to_archive` Run Draft、decision/summary 和 intake archive，`/api/agent-runs/:id/idea-manifest-draft` 会基于 run affected files/profile/skills/tags 生成不执行命令、不写盘的 `manifest_draft` message 与 `agent.run.idea_manifest_drafted` Activity，生成的 draft 默认带 `output/playwright/idea-work-*.md` 工作清单 artifact 与 `output/playwright/idea-agent-change-*.js` 本地 Agent file-change plan 两个 `fileChanges`、只读 work evidence、匹配 affected files 和 Agent JS 计划的验证命令，`Generate Manifest` + `Edit Manifest` 已串起 draft 预填；`/api/agent-runs/:id/idea-patch-manifest-draft` 与 `Generate Patch` 已补上 source patch manifest draft，默认 UI `useModel:false` 本地 fallback，API 可显式 `useModel:true` 加 `adapterId` 走现有模型 adapter，失败或输出不安全时记录 generation error 并降级本地 fallback；source patch manifest draft 现在附带 `patchQuality` 分数、等级、findings/blockers、验证命令和 evidence 覆盖信息，并写入 Run message 与 Activity；`/api/agent-runs/:id/idea-execution` 可记录 execution completion、verification tool result、`agent.run.idea_execution_completed` 和 final archive；最新又补 `AgentRunVerificationExecutor` 与 `/api/agent-runs/:id/idea-auto-execute`，只执行 allowlist 内 safe-root 文件改动、只读 work evidence 和本地验证命令，经 `PermissionGovernance` 判定后采集 work plan、file change evidence、work evidence、verification tool result、artifact metadata 并生成 final archive；Runs tab 已补 `Edit Manifest`，可从 UI 提交 JSON manifest；显式 `requiresApproval` 的文件改动首次执行会转 `deferred/approval_pending` 且不写盘，审批通过后带同一 `approvalId` 和同一 manifest 恢复写入；Governance Center 现已新增 Approval Actions、Preflight Review 与 approve-before-run diff gate，能对 deferred `idea_to_archive` run 展示待写文件、内容 hash、diff preview、验证命令、evidence 命令安全状态和 `Gate review-*`，点击 `批准并续跑` 前会重新拉取 preview 比对 gate，后端要求 `POST /api/agent-runs/:id/approval-resume` 携带匹配的 gate id/hash，缺失返回 428、过期/不匹配返回 409 且不调用 executor，成功时复用 run 内保存的 manifest 续跑并返回 `resumeReviewGate`；最新又把 approval resume gate audit 写入 run details、decision message、Activity 与 final archive evidence，Run detail 展示 `Approval Resume Gate`、gate id/hash、文件/命令/风险计数和命令摘要；`GET /api/agent-runs` 与 Runs tab 已支持按 `approvalResumeGateId` / `approvalResumeGateSha256` 反查对应 run；Activity Center 也能按 `reviewGateId` / `reviewSha256` 过滤审计流，在详情显示 `Approval Resume Gate` 面板并直接打开对应 Agent Run；最新再补 `GET /api/agent-runs/:id/approval-resume-gate-audit` 和 Run detail `Gate Audit Report`，把同一个 gate 在 run details、decision message、Activity、final archive evidence 四类来源中的 digest 做确定性对账，并支持 markdown 导出；当前又补 `POST /api/agent-runs/:id/approval-resume-gate-audit/archive` 与 Run detail `Archive Report`，把对账报告落盘到 `output/playwright/gate-audit-reports/*.md`，并把 report artifact 写入新的 Execution Archive evidence；expanded command policy 已允许 project-local `node --test`、exact allowlisted `npm run`、selected `node scripts/*` 与更完整只读 git evidence；权限治理支持高风险 HTTP/API 操作在人工批准后带 `approvalId` 安全重试；旧 SQLite `agent_runs` 表迁移顺序已修复；左侧栏入口已放大、分组并补名称标签。

最新补强（2026-05-26 07:00 CST）：approve-before-run diff gate 已升级为多文件 staged diff review。审批恢复 manifest 现在会为每个 fileChange 生成 `diffStats`（additions/removals/net/before/after lines）和 attention flags，并汇总为 `stagedDiffReview`（file/new/existing/blocked/operation/extension/attention/line totals + deterministic sha256）。该摘要进入 review gate fingerprint、`resumeReviewGateAudit`、Run detail、Activity gate panel、Gate Audit Report markdown 和 report archive evidence；Governance Center Preflight Review 显示 `Staged Diff +N/-M` 和每文件 `+/-`，e2e 已覆盖双文件 approval resume。

最新补强（2026-05-26 07:14 CST）：staged diff review 继续增加命令覆盖映射与风险排序。`buildApprovalResumeReview()` 会把验证命令和 work evidence 命令按文件路径映射到 fileChange，生成 `commandCoverage`、`coverageStatus`、verified/uncovered/evidence-only 计数、project-wide 覆盖计数、`riskScore`、`riskLevel`、`riskRank` 和 `topRiskFiles`；这些字段进入 staged diff sha 和 review gate fingerprint。Governance Center Preflight Review、Run detail、Activity gate panel 和 Gate Audit Report 现在显示 `2/2 verified`、`0 uncovered`、每文件 coverage/risk；Gate Audit Report 对账时只比较跨 Activity sanitize 稳定保留的 coverage/risk 摘要字段，避免深层展示数组被裁剪后误报 mismatch。

最新补强（2026-05-26 07:24 CST）：Governance Center Preflight Review 增加可操作 UI。文件列表按 `riskRank` 排序渲染为 `<details>`，每个文件显示 risk rank/level/score、coverage 状态、verify/evidence 计数和后端生成的 `riskReasons`；每个文件 coverage 命令以 chip 展示，点击后会高亮对应全局验证命令，完成从文件风险到具体命令的反跳。e2e 已覆盖折叠/展开、命令反跳和 Risk reasons。

最新补强（2026-05-26 07:33 CST）：Agent Run Session Timeline 增加 session evidence chain。`getSessionSnapshot()` 会把同一 `sessionId` 下的 run、message、tool result、archive、Activity、codebase question answer 和 approval resume gate 归并成顺序证据链，返回 item/kind/code answer/gate 计数、refs 与最近证据项；新增 `exportSession()` 和 `/api/agent-runs/session/:sessionId?format=markdown`，Run export 也附带 session evidence 摘要；Runs tab 在 Session Timeline 内展示 `Session Evidence Chain` 和 evidence kinds。e2e 已覆盖真实 UI。

最新补强（2026-05-26 07:42 CST）：command coverage 解释扩展完成。每个 staged diff fileChange 现在带 `coverageExplanations` / `coverageExplanationSummary`，能说明命令是直接命中文件、项目级验证、项目级 work evidence、只有 evidence 无 verification、完全 uncovered，还是文件本身被阻断；Governance Center 文件块新增 `Coverage explanation` 展开，Gate Audit Report markdown 增加 `Coverage Explanations` 计数和 per-file coverage reasons。为避免 Activity sanitize 裁剪导致 gate 对账误报，深层 explanations 不参与跨来源 digest，只参与 staged diff gate fingerprint、UI 和报告展示。

最新补强（2026-05-26 07:47 CST）：Session Evidence markdown UI 导出入口完成。Runs tab 的 Session Timeline 现在提供 `Export Session`，会调用 `/api/agent-runs/session/:sessionId?format=markdown` 并在本地 modal 中展示完整 markdown session evidence export；e2e 覆盖按钮、modal 内容、`Session Evidence Chain` 标题和同 session activity / sibling run 证据。

最新补强（2026-05-26 07:49 CST）：Preflight Review coverage filter UI 完成。Governance Center 的 Preflight Review 顶部新增 `Coverage filter`，由 `renderGovernanceCoverageFilter()` 渲染 `all`、`verified`、`project_wide_verified`、`evidence_only`、`uncovered`、`blocked` 六类覆盖状态计数按钮；文件块带 `data-gov-center-coverage`，筛选无命中时显示 `data-gov-center-coverage-empty`，不改变已有风险排序、折叠文件块、命令反跳和 coverage explanation。e2e 覆盖切换 `uncovered` 空状态与切回 `all`。

最新补强（2026-05-26 08:14 CST）：Session Evidence markdown 文件归档入口完成。`AgentRunStore.recordSessionEvidenceArtifact()` 会生成同一 session 的 markdown evidence export，写到 `output/playwright/session-evidence/agent-run-session-*.md`，记录 artifact path/size/sha256/sessionId/evidenceChainId，并通过既有 `recordArchive()` 挂入同 session run 的 Execution Archive；新增 `POST /api/agent-runs/session/:sessionId/archive`，Runs tab 的 Session Timeline 新增 `Archive Session`。该入口只写 safe-root 下的本地 markdown artifact，不执行命令，并校验指定 run 必须属于同一 session。

最新补强（2026-05-26 08:16 CST）：Codebase Index LSP-ish 类型契约证据完成。`JavaScriptAstAnalyzer` 现在为 method / type-method 输出 `owner` / `ownerType`，`CodeContextEvidence` sanitize 保留该字段；`SymbolGraph` 会把 class implements interface 的同名方法归并成 `type-implementation` 引用，Codebase citation / question answer coverage、persistent snapshot summary、Agent prompt summary 与前端 Symbol Graph / Local Code Answer 都会暴露 type implementation 计数。该切片不引入外部 LSP 服务、不上传代码、不调用模型，是后续 Tree-sitter/LSP 深化前可测试的类型契约证据。

最新补强（2026-05-26 09:18 CST）：P0-A Codebase Index 三期第一刀完成。`JavaScriptAstAnalyzer` / `CodeContextEvidence` 现在会记录 `dynamic-import` imports 与 references；`SymbolGraph` 新增 `routeTestChains` 和 `unresolvedReferences`，能把真实 API route definition、route handler 附近的 service/reference 调用、测试文件中的 route usage 串成 route-to-test citation path，同时输出 unresolved reference summary；`CodebaseCitationChain` 把 route-to-test chain 写入 citation `paths`，`CodebaseQuestionAnswer` coverage/limitations 会明确显示证据不足、未解析引用和 route-test 链路限制；Codebase Center / Agent preview 状态展示 parser coverage、route-test chain、unresolved refs，Local Code Answer UI 显示限制说明。该能力仍只使用本地 AST/FTS/vector/SymbolGraph，不上传代码、不调用模型。

最新补强（2026-05-26 09:42 CST）：P0-B Agent Run 归档 artifact 反查完成。`AgentRunStore.getTimeline()` 返回 archive artifact 聚合列表，`listArtifacts()` / `readArtifact()` 只允许读取已记录且位于 `output/playwright/session-evidence/` 或 `output/playwright/gate-audit-reports/` 下的 markdown artifact；新增 `GET /api/agent-runs/:id/artifacts` 与 `GET /api/agent-runs/:id/artifacts/:artifactId/download`；Run detail 新增 `Execution Artifacts` 列表，支持复制路径和打开 markdown；Activity detail 新增 `Archive Artifacts`，可从 `agent.run.archived` 事件反查并打开 artifact。单测覆盖 safe path、缺失文件、非允许目录和未记录路径拒绝；e2e 覆盖 Gate Audit Report artifact、Session Evidence artifact 与 Activity reverse lookup。

最新补强（2026-05-26 09:55 CST）：P0-C Gate 审计对账分区完成第一刀。Gate Audit Report 保留 canonical digest，同时新增 `file/command/risk/coverage/artifact` 分区 digest；mismatch 输出 `partition`、`reason`、expected/actual partition digest 与缺失字段，能明确哪个 source 缺字段或不一致。`coverageExplanations` 不进入跨来源 coverage digest，只作为报告明细；分区 digest 避开 Activity sanitize 会裁剪的展示数组。单测覆盖 file、command、risk、coverage、artifact 五类 mismatch，e2e 覆盖报告中的 `Partition Mismatches` 展示。

最新补强（2026-05-26 10:07 CST）：P1 产品主路径第一刀完成。Agent Center Dispatch Preview 顶部新增 `Idea-to-Archive Path` 引导条，串联 `Idea -> Code Context -> Dispatch Preview -> Run Draft` 并显示下一步；预演完成后局部刷新到 `Next: 创建 Run Draft`，输入/角色/文件变化会清空旧 classification。`idea_to_archive` Run detail 新增同名主路径块，把 `Idea -> Dispatch -> Manifest/Patch -> Work + Verify -> Preflight -> Archive` 作为单条任务流展示，并按 manifest draft、patch quality、approval defer、gate audit、final archive、artifact 状态显示下一步操作。e2e 覆盖 guided dispatch path、guided run path、final archive、approval resume 和 artifact lookup/open，未改变本地治理边界。

最新补强（2026-05-26 10:16 CST）：P1 产品主路径第二刀完成。Run detail 的 `Idea-to-Archive Path` 现在会给出 `Recommended next` 主按钮和 `Other actions` 次级按钮；主按钮按状态自动指向 `Generate Manifest`、`Auto Work + Verify`、`Open Preflight Review`、`Gate Audit Report` 或 `Archive Run`。新增 `data-agent-run-governance-review`，审批挂起时可从 Run detail 直接打开 Governance Center 的 Preflight Review，并高亮同 run 的续跑审批项。e2e 覆盖 recommended next 生成 manifest、生成后进入 auto verify、patch alternative 和 preflight action。

最新补强（2026-05-26 10:22 CST）：P1 产品主路径第三刀完成。Idea-to-Archive 专属动作不再重复显示在 Run detail 顶部 action bar，而是集中在 `Idea-to-Archive Path` 的 `Recommended next` / `Other actions`；顶部 action bar 只保留审批/委派/预算、Replay、Activity 等通用入口。非 `idea_to_archive` run 的 `Archive Run` 仍保留在顶部。e2e 覆盖 action bar 去重、主路径 final archive action 和非 Idea Run 归档旧链路。

最新补强（2026-05-26 10:27 CST）：P1 产品主路径第四刀完成。`Idea-to-Archive Path` 在 final archive 状态下会显示 `Final archive` 摘要，包括归档 summary、状态、tools/files/artifacts/blockers 计数；推荐动作改为 `Review Archive`，点击聚焦 `Execution Archive`，`Add Archive Note` 和 `Open Artifacts` 作为次级动作保留。e2e 覆盖 final archive 摘要和 Review Archive 聚焦。

最新补强（2026-05-26 10:43 CST）：P1 Model / Skill Center 收口完成。Agent Center `Models/Skills` tab 聚合本地 provider 状态、前端模型候选清单、enabled skills、profile skill coverage、missing bindings、dispatch hint 缺口、只读 provider health 边界、`Model Recommendations` 推荐来源和 `Skill Source & Risk` 来源/风险统计；界面明确标注 `Local status only · no secrets shown · provider config is read-only here`，provider 状态显示 `No live ping`，只读取 `/api/room-adapters/providers`、`/api/skills`、`/api/agent-registry`，不写 provider 配置、不展示密钥、不引入云账号或 SaaS 租户。e2e 覆盖 Agent Center 新 tab、Provider Model Status、Model Recommendations、Skill Injection Matrix、Skill Source & Risk 和无明文 API key 展示。

## 后续详细计划摘要

完整计划以 `任务交接.md` 的「后续详细计划」为准。下个执行窗口不要重新泛泛规划，优先继续 P1 产品主路径深化，或回到 P0-A Tree-sitter/LSP 级证据深化；P0-B 已完成，P0-C 第一刀已满足验收，P1 第一刀已把主路径可视化串起。

1. **P1 后续**：产品主路径的 Idea -> Dispatch -> Manifest/Patch -> Work + Verify -> Preflight -> Archive 已有 guided path、Recommended next、action 去重和 final archive 摘要；Model / Skill Center 已有只读本地状态、推荐来源和 Skill 来源/风险统计。下一步可继续补真实无副作用 health check 历史、最近错误摘要、usage analytics，或回到 P0-A Tree-sitter/LSP 级证据深化。
2. **P0-A Codebase Index 三期后续**：继续在现有 AST/FTS/vector/snapshot/citation/code question/type implementation/dynamic import/route-to-test/unresolved refs 基础上补 Tree-sitter/LSP 级证据、跨文件/跨语言引用。验收必须包含定向单测、`npm run lint`、`npm test`、`git diff --check`，以及 Codebase Center e2e/替代实机验证。
3. **P0-C Gate 审计对账细化**：第一刀已完成。后续只做更细字段级 diff 或 UI drill-down，不再重复基础 partition digest。
4. **P0-B Agent Run 归档 artifact 反查**：已完成。后续只做稳定化或与 P0-C artifact digest 分区衔接，不再重复实现基础 lookup/download。
5. **P1 Model / Skill Center**：已显示 provider/model/skill 本地状态、Model Recommendations、Skill Injection Matrix 与 Skill Source & Risk；后续补 provider 最近错误、无副作用 health check 历史和 usage analytics，不引入云账号或 SaaS 租户。
6. **P2 权限治理 UI 闭环**：对 plugin/MCP/webhook/provider 等非 Agent Run 高风险入口补 approvalId 安全重试，必须绑定原 action/target，不自动重放危险 shell。
7. **P3 稳定化与提交拆分**：当前未提交增量很大，提交时按 Codebase Index 后端、Agent Run 后端、前端 UI、e2e/文档拆分，禁止 `git add .`。
8. **P4-P9**：本地 evidence 知识库、Governance 工作队列、性能资源控制、i18n/accessibility、发布准备、安全审计。

阶段准入规则：每个阶段必须完成实现、定向测试、全量 lint/test/diff check、必要 e2e、文档交接和自检；UI 改动必须真实启动面板跑 e2e 并确认端口停止。

## 给下个对话框的启动提示

请先进入这个项目：

```bash
cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板
pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short
```

然后阅读：

1. `任务交接.md`
2. `上下文交接.md`
3. `Claude交接提示.md`
4. `docs/local-governance-learning-record.md`
5. `docs/README.md`
6. `docs/xikelab-agent-skill-registry.md`
7. `docs/crow5-deep-analysis-personal-growth-plan.md`
8. `docs/crow5-local-research-evidence-map.md`
9. 最近提交：`git log --oneline --decorate -8`

当前任务背景：这个项目是 Xike Lab / Claude 可视化面板，一个本地可控的多 AI Agent 开发面板。最近重点不是泛泛分析 Paperclip，而是把 Paperclip 中适合 Xike Lab 的本地治理能力吸收为可运行功能：预算、审批、审计、委派、Autopilot 自启动和治理面板。

## 已完成的关键提交

最新提交：

```text
6010a23 侧边栏: 放大并分组功能入口
a9e1a52 权限治理: 支持审批后安全重试
ab7d007 文档: 更新 Crow5 吸收与本地治理交接
0d6f72f 权限治理: 统一判定高风险操作
f027f3a Agent 工作台 UI: 展示 Runs 与 Codebase 查询
632ad95 Agent 运行时: 接入 Registry、Run 与代码索引后端
f8b3c81 治理面板: 接入本地治理与委派自启动
```

这些提交已把本轮 Paperclip/Crow5 学习吸收内容和后续 UI/权限增量固化到 git。不要用 `git reset --hard`、大范围 checkout 或删除文件来“清理”工作区。

当前 `main` / `origin/main` 位置：

```text
b27f3f0 修复：Gemini 配额耗尽自动降级 + abort 不计入断路器 (#33)
```

也就是说当前分支是在 PR #33 合并后的主线基础上继续做的本地治理分支。

## Paperclip 学习内容是否还在

已确认还在。核心记录文件：

- `docs/local-governance-learning-record.md`
- `docs/README.md`

`docs/local-governance-learning-record.md` 明确记录：

- 学习 Paperclip 的本地治理方向
- Xike Lab 没有变成 Paperclip clone，也没有引入 Paperclip 运行时依赖
- 已吸收的能力包括预算治理、审批、审计、委派、Autopilot 自启动
- 后续如果要求“去 Paperclip 标签”，默认只移除品牌/来源表述，不删除已经落地的功能

## 已落地能力

### 0. Agent/Skill Registry 与可追溯诊断（已提交增量）

关键文件：

- `src/agents/AgentSkillRegistry.js`
- `src/agents/AgentPolicyStore.js`
- `src/agents/CodebaseMap.js`
- `src/agents/CodebaseIndexStore.js`
- `src/agents/CodebaseQueryEngine.js`
- `src/agents/CodebaseFtsIndex.js`
- `src/agents/CodebaseVectorIndex.js`
- `src/agents/CodebasePersistentIndex.js`
- `src/agents/CodebaseCitationChain.js`
- `src/agents/SymbolGraph.js`
- `src/agents/JavaScriptAstAnalyzer.js`
- `src/agents/AgentRunStore.js`
- `src/agents/AgentRunVerificationExecutor.js`
- `src/permissions/PermissionGovernance.js`
- `src/server/routes/agentRegistry.js`
- `src/server/routes/agentRuns.js`
- `src/server/routes/codebaseIndex.js`
- `src/agents/CodeContextSignals.js`
- `src/agents/CodeContextEvidence.js`
- `src/audit/ActivityLog.js`
- `src/server/routes/activity.js`
- `src/metrics/MetricsStore.js`
- `public/app.js`
- `public/style.css`
- `docs/xikelab-agent-skill-registry.md`

能力摘要：

- 新增 8 个 Xike Agent Profile、dispatch tag、Skill 绑定和 prompt runtime context。
- 支持本地 profile 治理策略覆盖，写入 `~/.claude-panel/agent-policies.json`。
- 房间成员可显式绑定 Agent Profile；房间可绑定已安装启用的 Skill。
- Skill 注入保留来源解释：`profile`、`dispatch:<tag>`、`room`。
- Skill 诊断会识别过多 Skill、prompt 体量过大和 frontmatter 冲突。
- 诊断写入 `metrics.recorded`，非空时额外产生 `agent.skill_diagnostics` 审计事件。
- Activity Center 已支持 `agentOnly`、`agentProfileId`、`skillName`、`diagnosticCode` 查询与 `Agent/Skill`、`诊断`、`Metrics` 快捷筛选，并在详情区显示紧凑诊断摘要。
- 新增工程上下文分派信号层：可从受影响文件路径推导 `implementation`、`architecture`、`verification`、`design`、`governance`、`release` 等 dispatch tag。
- 新增 source evidence 层：从变更文件提取 JS/TS symbols、imports、API routes、测试 anchors、HTML id、CSS selector、Markdown heading，作为后续 Tree-sitter/LSP/VectorIndex 的可替换前置层。
- JS 文件的 source evidence 已从 regex POC 升级为 Acorn AST 优先；TS/TSX/JSX 通过 `@babel/parser` 解析：记录 parser、diagnostics、symbols/imports/exports/anchors/references、import specifiers 和 export bindings；类型导入、类型引用、interface extends、class implements、泛型 constraint、TS assertion、TS satisfies、泛型 instantiation 保留为 `type-import`、`type-reference`、`type-extends`、`type-implements`、`type-constraint`、`type-assertion`、`type-satisfies`、`type-instantiation`，成员调用/访问保留为 `member-call`、`member-reference`；解析失败或其他文本语言继续走 fallback。
- 新增 Codebase Map POC：扫描当前工程文本代码文件，按任务 query 选出 focus files，并生成 evidence、signals 和轻量 import graph。
- 新增 Symbol Graph POC：从 focus files 的 symbols 建立 definitions / references / calls，并把 `/api/...` route anchors 与前端 API 使用关联起来；AST 文件按“同文件定义优先、显式 import/export 绑定次之、唯一全局定义兜底”减少同名 helper 串错，已覆盖 named/default/re-export/renamed import，并保留 `type-*` / `member-*` reference kind 区分类型关系、成员访问与运行时调用。
- Agent 图谱 Dispatch Preview 已支持粘贴受影响文件路径、读取当前 git 变更或构建工程地图，结果会展示 `Code Context`、`Codebase Map`、`Symbol Graph`、`Code Evidence`、`codeScore`、触发理由和路径。
- `metrics.recorded` 会保留 `agentCodeContextSignals` 与 `agentCodeContextEvidence`，让真实运行后的工程上下文分派依据可审计。
- Agent Run 会话化一期已落地：`agent_runs`、`agent_messages`、`agent_tool_results` 三张表进入 SQLite；`MetricsStore.record()` 在 server runtime 中会同步生成 metric 来源的 finished agent run；`POST /api/delegations/:id/autostart` 会同步生成 `delegation_autostart` 来源的 queued agent run；`/api/agent-runs` 支持 run 列表、timeline、message/tool-result 追加、状态迁移和 `format=json|markdown` 导出。
- Agent Run 底座已补上治理关联字段：`deferReason`、`approvalId`、`budgetIncidentId`、`delegationId`、`relatedActivityIds`；run 创建、状态迁移、message 追加、tool result 记录会写入 `agent_run` Activity，后续可从 Activity 反查 run。
- Agent Run 生命周期最小闭环已接入 `RoomAdapter.chat()`：adapter 调用开始创建 running run；预算 hard stop 转 `deferred` / `budget_blocked`；成功转 `succeeded`；失败转 `failed`；abort/cancel 转 `cancelled`。adapter result/error 会携带 `agentRunId`，`MetricsStore.record()` 复用该 run 追加 metric message，避免重复生成 metrics-only run。
- Agent Run 治理链路已打通：budget usage / incident、approval payload、delegation payload、Autopilot job payload、`delegation.autostart` Activity 都会写入同一个 `agentRunId`；Autostart handler 在 `approval_pending`、`budget_blocked`、成功创建/启动目标房时推进同一个 run；run export 会聚合 approval / delegation / autopilot / budget 相关 Activity。
- Agent Run 会话化补强未提交增量：`AgentRunStore.getTimeline()` 现在直接返回 `activityEvents`、`governanceLineage` 与 `archives`，聚合 approval、delegation、budget incident、autopilot job、blockers 和 nextAction；`GET /api/agent-runs` 支持 `deferReason` 与 `hasGovernance` 过滤；Runs tab 增加 sourceType、approvalId、delegationId、budgetIncidentId、deferReason、治理链过滤，并在详情中展示 Governance Chain、Replay Plan、Replay Result、Archive Run、Execution Archive 与预算 Activity 入口；`recordReplayPlan()` 会写入 `replay_plan` message 与 `agent.run.replay_planned` Activity；`recordReplayResult()` 会写入 `replay_result` message 与 `agent.run.replay_result_recorded` Activity；`recordArchive()` 会写入 `archive` message 与 `agent.run.archived` Activity，汇总来源、skills/tags、治理链、tool result 统计、失败工具、证据 ID 和涉及文件；`createIdeaRun()` 与 `POST /api/agent-runs/idea` 会从 Dispatch Preview 的 idea/classification/code context 创建 queued `idea_to_archive` Run Draft，写入 decision + summary + intake archive，并记录 `agent.run.idea_intake_created`；`recordIdeaManifestDraft()` 与 `POST /api/agent-runs/:id/idea-manifest-draft` 会生成安全 manifest draft，只记录 `manifest_draft` message 与 `agent.run.idea_manifest_drafted` Activity，不写盘、不执行命令，draft 默认包含 `output/playwright/idea-work-*.md` 工作清单 `fileChanges`、只读 work evidence 与 affected files 匹配验证命令，Runs tab `Generate Manifest` 后 `Edit Manifest` 会用最新 draft 预填；`completeIdeaRun()` 与 `POST /api/agent-runs/:id/idea-execution` 会把未完成 draft 转为 running，记录 execution summary、verification tool result、final status、`agent.run.idea_execution_completed` 和 final archive，已完成 run 不允许重复完成；`AgentRunVerificationExecutor` 与 `POST /api/agent-runs/:id/idea-auto-execute` 会解析安全 manifest，仅允许 safe-root 文本文件 create/update/append，`git status --short`、`git status --porcelain=v1`、`git diff --name-only`、`git diff --stat` 等只读 work evidence，`git diff --check`、`npm test`、exact allowlisted `npm run`、`node --check`、project-local `node --test`、selected `node scripts/*` 等本地验证命令，先经 `PermissionGovernance` 判定，再用非 shell 子进程或 `file.write` 执行并把 passed/failed/blocked/approval-required 结果写入 file change evidence、work evidence、verification tool result 与 final archive；Runs tab 已新增 `Auto Work + Verify`、`Generate Manifest` 与 `Edit Manifest`，详情会显示 `work_plan` / `manifest_draft` message、file changes 与 artifacts 计数；审批型 file change 会先进入 `deferred/approval_pending`，批准后同一 manifest + `approvalId` 可续跑。Activity 支持 `agentRunId` 过滤和反向打开具体 Run，不自动重放危险命令或非 allowlist 命令。
- Agent Run session-level 聚合已补上：`AgentRunStore.getSessionSnapshot()` 和 `GET /api/agent-runs/session/:sessionId` 会聚合同一 session 下的 runs、messages、tool results、archives、Activity、status/source/profile counts、session-level governance blockers 与 next actions；`GET /api/agent-runs/:id?includeSession=true` 会把这个聚合带回详情；Runs tab 新增 `sessionId` 筛选，Run 详情展示 `Session Timeline` 并可反跳同一 session 的 sibling run。
- Codebase Index 二期后端已落地：`CodebaseIndexStore` 提供 rebuild / status / query / question，本地缓存 Codebase Map；最新未提交增量增加按 cwd 隔离的 per-file evidence cache，`CodebaseMap` 在 rebuild 时基于文件 `mtime` / `size` / 内容 hash 复用未变化文件，只重算新增或变更文件，并在 status 返回 `cacheStats`；新增 `CodebaseFtsIndex` 将 focus evidence 写入内存 SQLite FTS5 表并使用 `bm25()` 排序，query 会合并带 `fts5`、`bm25`、`sqlite-fts` reason 与 `bm25Rank` 的命中，并返回 `ftsSummary`；新增 `CodebaseVectorIndex` 复用本地 `hashEmbed`，把 evidence 文件压成内存 hash-vector，以 cosine 相似度生成 `local-hash-vector/semantic-vector/vector-index` 信号并返回 `semanticScore` / `vectorSummary`；query cache 有上限并会关闭被淘汰的内存 FTS 句柄；新增 `CodebasePersistentIndex` 将 rebuild 后的 status/map 写入本地 SQLite `codebase_index_snapshots`，默认每 cwd 保留 48 个 query 快照，`useSnapshot=true` 可在冷启动时从快照重建内存 FTS 与 vector index；新增 `CodebaseCitationChain` 为 query result 附加 symbols/anchors/snippets/imports/exports/references 与 Symbol Graph 证据链；新增 `CodebaseQuestionAnswer` 把 query/citation 结果压成本地 code answer，返回 answer、answerLines、citations、coverage、limitations；`JavaScriptAstAnalyzer` 覆盖 TS/TSX/JSX 与 `type-import` / `type-reference` / `type-extends` / `type-implements` / `type-constraint` / `type-assertion` / `type-satisfies` / `type-instantiation` / `member-call` / `member-reference`；`SymbolGraph` 支持 import/export 绑定并保留 `type-*` / `member-*` reference kind；`CodebaseQueryEngine` 负责 query token 扩展、中文治理问题映射、源码优先排序、symbol / anchor / snippet / reference 打分；`/api/codebase-index/rebuild`、`/api/codebase-index/status`、`/api/codebase-index/query`、`/api/codebase-index/question` 已注册到 server。
- Crow5 本地研究证据地图已落地：`docs/crow5-local-research-evidence-map.md` 基于本机可访问内容记录 Crow5 v1.3.13 的来源/签名/许可边界、bundle 与用户数据结构、默认 `default-opencode.jsonc`、Agent/Skill/dispatch、SQLite schema、日志元信息、UI 布局、能力清单、合法可借鉴模式、Xike Lab 自研替代方案、P0-P9 路线，以及无需用户授权可继续确认的事项。
- Codebase Index 查询结果保持可解释：返回 `path`、`line`、`score`、`reason`、`anchor`、`parser`、`symbols`、`routes`、`citation` 和 `citationSummary`；question 结果额外返回 deterministic `Local Code Answer` 与 `C1...` citations，不做黑盒 AI 总结；Codebase Answer 现在可添加到 Dispatch Preview，进入 prompt preview 与 Idea Run details / intake archive / Run detail；扫描继续跳过 `.git`、`node_modules`、构建产物、`public/vendor`、二进制和超大文件，同时把上限调到能覆盖当前 393KB 的 `public/app.js`，用于回答 Agent 图谱 DOM/handler 问题。
- Codebase Index 5 问 smoke 已验证能命中实现文件：`RoomAdapter` 预算处理、`MetricsStore` / `ActivityLog` 诊断写入、`DelegationAutostart` 链路、`public/app.js` 的 `btnAgentRegistry` handler、`SymbolGraph` route usage 聚合。
- Agent Center 最小 UI 已落地：`🧠 Agent 图谱` 弹窗拆成 `Profiles` / `Dispatch` / `Runs` / `Policies` 四个 tab；默认保留 Dispatch Preview，Runs tab 接入 `/api/agent-runs`、timeline 和 export activity，显示 status、room/session/task、profile、token/cost/latency、diagnostics、messages、tool results，并可跳转审批、委派和 Activity。
- Codebase Center 最小 UI 已落地：顶栏 `⌘` 入口打开本地代码索引面板，显示 index status、Rebuild、Query、Answer、Local Code Answer、results、reason/anchor/snippet/symbols/routes；结果可逐条或批量添加到 Agent Center 的 Dispatch Preview，转成 `affectedFiles` 和最小 `codeContextEvidence`，不做黑盒 AI 总结、不做云索引。
- 权限治理扩展已落地统一判定函数：`evaluatePermission({ actorType, agentRunId, roomId, action, target, cwd, risk })` 返回 `allow/ask/deny + reason + approvalPayload`，并记录 `permission.decision` Activity；带 `agentRunId` 时会追加 Agent Run `decision` message。
- 已接入高风险入口：Claude `Bash/Write/Edit/MultiEdit/Read/LS/Glob/Grep` tool_use、plugin install/delete/reload/exec、MCP create/update/delete/test/list tools/resources/prompts、Room Adapter/Watcher provider-model 配置写入、Watcher provider access 测试、Webhook create/update/test 出站上传、报告 outputPath 写入。
- 权限覆盖范围：危险 shell、文件写/删、外部目录访问、敏感 `.env` 读取、skill/plugin 执行与配置、provider/model config 访问与写入、network upload、auto-accept scope。`deny` 不执行；`ask` 创建 manual approval 并返回 202；私网/loopback upload 和敏感目录直接拒绝。
- 审批后安全重试闭环已补：HTTP/API 高风险入口可从 body/query/header 读取 `approvalId`，只有 action 和 target 与原审批 payload 完全匹配且状态为 `approved` 时才转 `allow`；pending 复用原审批不重复创建；rejected/cancelled/mismatch 直接 `deny`。MCP/Webhook/plugin install 写入前会剥离 `approvalId` 等治理字段，避免污染业务配置。危险终端命令仍不自动重放。
- 旧 SQLite `agent_runs` 表迁移顺序已修复：先补 `delegation_id` / `approval_id` / `source_type` 等列，再创建相关索引，避免旧库访问 `/api/activity`、`/api/agent-runs`、`/api/delegations` 时报 `no such column: delegation_id`。
- 左侧栏入口已放大并分组：`工作台`、`治理`、`系统` 三组，两列大按钮，每个入口有可见名称；窄屏保持单列图标；`btnInspectorToggle` 只更新图标，不再清空“右栏”标签。

### 1. 预算治理

关键文件：

- `src/budget/BudgetPolicyStore.js`
- `src/server/routes/budgets.js`
- `src/metrics/MetricsStore.js`
- `src/room/RoomAdapter.js`
- `tests/unit/budget-policy-store.test.js`
- `tests/unit/routes/budgets-routes.test.js`
- `tests/unit/room-adapter-budget.test.js`

能力摘要：

- 支持预算策略存储
- 支持成本事件和预算事件记录
- 支持预算命中后中断/拦截
- 和 RoomAdapter / metrics 有集成

### 2. 审批治理

关键文件：

- `src/approval/ApprovalStore.js`
- `src/approval/CommandApprovalGate.js`
- `src/server/routes/approvals.js`
- `tests/unit/approval-store.test.js`
- `tests/unit/command-approval-gate.test.js`

能力摘要：

- 高风险命令可进入审批队列
- 支持审批记录去重
- 支持 approval gate 暂停危险操作
- 为后续前端审批中心和 Autopilot gate 提供基础

### 3. 审计事件

关键文件：

- `src/audit/ActivityLog.js`
- `src/server/routes/governance.js`
- `tests/unit/routes/governance-routes.test.js`

能力摘要：

- 本地结构化事件记录
- 治理概览接口可汇总审批、预算、委派等待办
- 后续可扩展为 timeline / ticket viewer / error trace / cost trace

### 4. 委派与治理面板

关键文件：

- `src/delegation/DelegationStore.js`
- `src/server/routes/delegations.js`
- `public/app.js`
- `tests/unit/routes/delegations-routes.test.js`

能力摘要：

- 支持委派任务记录
- 支持从 Delegation Center 发起委派自启动
- 前端有 `审批后自启动` 入口
- 后端提供 `POST /api/delegations/:id/autostart`

### 5. Autopilot 审批后自启动

关键文件：

- `src/autopilot/DelegationAutostart.js`
- `src/autopilot/AutopilotScheduleStore.js`
- `src/autopilot/AutopilotScheduler.js`
- `server.js`
- `tests/unit/delegation-autostart.test.js`
- `tests/unit/autopilot-scheduler.test.js`

能力摘要：

- 新增 `start_delegation` Autopilot job action
- 委派任务先创建/复用审批
- 审批未通过时 job defer，不计失败
- 审批通过后自动启动目标房间
- 预算超限时阻断，不继续消耗模型调用
- `server.js` 已注册 `makeDelegationAutostartHandler`

## 已验证记录

当前 staged diff review 增量验证（2026-05-26 07:00 CST）：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-review-... PORT=52085 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52085/?t=... npm run test:e2e
lsof -nP -iTCP:52085 -sTCP:LISTEN
```

结果记录：

- 定向测试：4 个文件通过，13 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：102/102 通过，服务已停止，52085 无监听；覆盖 Governance Center 多文件 `Staged Diff +2/-0`、双文件 approval resume、Run detail staged diff、Activity gate audit、Gate Audit Report 与 report archive artifact。

当前 staged diff coverage/risk 增量验证（2026-05-26 07:14 CST）：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-coverage-... PORT=52086 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52086/?t=... npm run test:e2e
lsof -nP -iTCP:52086 -sTCP:LISTEN
```

结果记录：

- 定向测试：4 个文件通过，13 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：102/102 通过，服务已停止，52086 无监听；覆盖 Governance Center coverage/risk、Run detail staged diff coverage、Gate Audit Report coverage 和 report archive artifact。

当前 staged diff 可操作 UI 增量验证（2026-05-26 07:24 CST）：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-ui-... PORT=52087 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52087/?t=... npm run test:e2e
lsof -nP -iTCP:52087 -sTCP:LISTEN
```

结果记录：

- 定向测试：4 个文件通过，13 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：103/103 通过，服务已停止，52087 无监听；覆盖文件折叠、coverage command chip 高亮和 Risk reasons。

当前 Agent Run session evidence chain 增量验证（2026-05-26 07:33 CST）：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-evidence-home PORT=52088 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52088/?t=... npm run test:e2e
lsof -nP -iTCP:52088 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，10 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：104/104 通过，服务已停止，52088 无监听；覆盖 Session Evidence Chain、evidence kinds 和 `run:2` 真实 UI 展示。

当前 command coverage 解释扩展增量验证（2026-05-26 07:42 CST）：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/unit/agent-run-approval-resume-review.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-coverage-explain-home-3 PORT=52091 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52091/?t=... npm run test:e2e
lsof -nP -iTCP:52089 -sTCP:LISTEN
lsof -nP -iTCP:52090 -sTCP:LISTEN
lsof -nP -iTCP:52091 -sTCP:LISTEN
```

结果记录：

- 定向测试：4 个文件通过，13 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：104/104 通过，服务已停止，52089/52090/52091 无监听；覆盖 Coverage explanation 展开、safe verification 解释、Gate Audit Report Coverage Explanations。
- 闭环修复：首次 e2e 为 103/104，原因是深层 coverage explanations 被纳入 gate audit digest 后 Activity sanitize 裁剪造成 mismatch；已改为 digest 只比较稳定摘要字段，解释仍保留在 UI/报告/staged diff gate。

当前 Session Evidence markdown UI 导出入口增量验证（2026-05-26 07:47 CST）：

```bash
node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs && npm test -- tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-store.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-export-home PORT=52091 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52091/?t=... npm run test:e2e
lsof -nP -iTCP:52091 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，10 个测试通过。
- 全量测试：62 个文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：106/106 通过，服务已停止，52091 无监听；覆盖 Runs tab Session Timeline `Export Session`、markdown modal 标题、`Session Evidence Chain` 和同 session activity / sibling run 证据。

当前 Preflight Review coverage filter UI 增量验证（2026-05-26 07:49 CST）：

```bash
node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs && npm run lint && npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/routes/governance-routes.test.js
npm test
git diff --check
HOME=/tmp/xikelab-coverage-filter-home PORT=52092 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52092/?t=... npm run test:e2e
lsof -nP -iTCP:52092 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，3 个测试通过；lint 通过。
- 全量测试：62 个文件通过，320 个测试通过。
- diff check：通过。
- e2e：107/107 通过，服务已停止，52092 无监听；覆盖 Governance Center coverage filter、Agent Run session export action 和 session evidence export。

当前 Session Evidence markdown 文件归档入口增量验证（2026-05-26 08:14 CST）：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-archive-home PORT=52093 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52093/?t=... npm run test:e2e
lsof -nP -iTCP:52093 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，10 个测试通过；语法检查通过。
- 全量测试：62 个文件通过，321 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，服务已停止，52093 无监听；覆盖 Session Timeline `Archive Session` 和 `Session evidence archived:` artifact 展示。

当前 Codebase Index 类型契约证据增量验证（2026-05-26 08:16 CST）：

```bash
node --check src/agents/CodeContextEvidence.js && node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseCitationChain.js && node --check src/agents/CodebaseQuestionAnswer.js && node --check src/agents/CodebasePersistentIndex.js && node --check src/agents/AgentSkillRegistry.js && node --check public/app.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/symbol-graph.test.js && node --check tests/unit/codebase-index-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/agent-skill-registry.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-type-implementation-home PORT=52092 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52092/?t=... npm run test:e2e
lsof -nP -iTCP:52092 -sTCP:LISTEN
```

结果记录：

- 定向测试：6 个文件通过，40 个测试通过。
- 全量测试：62 个文件通过，321 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，服务已停止，52092 无监听；覆盖 Agent preview Symbol Graph `type impl` 计数。
- 闭环修复：首次定向测试发现 `CodeContextEvidence` sanitize 裁掉 method owner 字段，导致 `SymbolGraph` 无法生成 type implementation 引用；已保留 `owner` / `ownerType` 并复测通过。

当前 P0-A Codebase Index dynamic import / route-to-test / unresolved refs 增量验证（2026-05-26 09:18 CST）：

```bash
node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseCitationChain.js && node --check src/agents/CodebaseQuestionAnswer.js && node --check public/app.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/code-context-evidence.test.js && node --check tests/unit/symbol-graph.test.js && node --check tests/unit/codebase-index-store.test.js
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0a-codebase-home PORT=52094 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0a-codebase-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52094 npm run test:e2e
lsof -nP -iTCP:52094 -sTCP:LISTEN
```

结果记录：

- 定向测试：5 个文件通过，25 个测试通过。
- 全量测试：62 个文件通过，323 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，服务已停止，52094 无监听；覆盖 Codebase Center parser coverage、Local Code Answer limitations、route-to-test chain / unresolved reference 单测。

当前 P0-B Agent Run 归档 artifact 反查增量验证（2026-05-26 09:42 CST）：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0b-artifact-home-fresh PORT=52096 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0b-artifact-home-fresh PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52096 npm run test:e2e
lsof -nP -iTCP:52096 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，11 个测试通过。
- 全量测试：62 个文件通过，324 个测试通过。
- lint / diff check：通过。
- e2e：115/115 通过，服务已停止，52096 无监听；覆盖 Gate Audit Report artifact lookup/open、Activity archive artifact reverse lookup、Session Evidence artifact lookup/open。

当前 P0-C Gate 审计对账分区增量验证（2026-05-26 09:55 CST）：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0c-gate-audit-home PORT=52097 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0c-gate-audit-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52097 npm run test:e2e
lsof -nP -iTCP:52097 -sTCP:LISTEN
```

结果记录：

- 定向测试：2 个文件通过，12 个测试通过。
- 全量测试：62 个文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：115/115 通过，服务已停止，52097 无监听；覆盖 Gate Audit Report `Partition Mismatches` 展示。

当前 P1 产品主路径第一刀增量验证（2026-05-26 10:07 CST）：

```bash
node --check public/app.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-main-path-home-clean PORT=52098 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-main-path-home-clean PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52098 npm run test:e2e
lsof -nP -iTCP:52098 -sTCP:LISTEN
```

结果记录：

- 全量测试：62 个文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：117/117 通过，服务已停止，52098 无监听；覆盖 guided dispatch path、guided run path、final archive、approval resume gate audit、Gate/Session artifact lookup/open。
- 闭环修复：首次 e2e 发现 Dispatch Preview 预演后主路径条未刷新，已新增局部刷新函数并在输入变化时清空旧 classification，干净 HOME 复测通过。

当前 P1 产品主路径第二刀增量验证（2026-05-26 10:16 CST）：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-main-next-home PORT=52099 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-main-next-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52099 npm run test:e2e
lsof -nP -iTCP:52099 -sTCP:LISTEN
```

结果记录：

- 全量测试：62 个文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：121/121 通过，服务已停止，52099 无监听；覆盖 recommended next manifest、verify、patch alternative、preflight action。
- 自检：没有改后端执行权限或审批 gate 规则；账号、密钥、登录、付费授权不涉及。

当前 P1 产品主路径第三刀增量验证（2026-05-26 10:22 CST）：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-dedupe-home PORT=52100 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-dedupe-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52100 npm run test:e2e
lsof -nP -iTCP:52100 -sTCP:LISTEN
```

结果记录：

- 全量测试：62 个文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：123/123 通过，服务已停止，52100 无监听；覆盖 Idea-to-Archive action bar 去重、guided final archive action 和通用 Agent Run archive。
- 自检：没有删除 Idea-to-Archive 能力，只移动入口；没有改变后端权限、审批 gate 或 artifact 安全规则。

当前 P1 产品主路径第四刀增量验证（2026-05-26 10:27 CST）：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-final-archive-home PORT=52101 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-final-archive-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52101 npm run test:e2e
lsof -nP -iTCP:52101 -sTCP:LISTEN
```

结果记录：

- 全量测试：62 个文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：124/124 通过，服务已停止，52101 无监听；覆盖 final archive summary 和 Review Archive 聚焦。
- 自检：只增强 UI 汇总和滚动聚焦，不改变 archive 数据结构、artifact 安全下载或审批 gate 规则。

当前提交后最新验证：

```bash
npm test
npm run lint
git diff --check
npm run test:e2e
```

结果记录：

- 全量单测：60 个文件通过，291 个测试通过。
- lint：通过。
- diff check：通过。
- e2e：63/63 通过。
- 可视复查截图：`output/playwright/sidebar-nav-redesign-desktop.png`。

当前未提交增量最新验证（AST 优先 Symbol Graph 后）：

```bash
node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/SymbolGraph.js && node --check src/agents/AgentSkillRegistry.js && node --check public/app.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/code-context-evidence.test.js && node --check tests/unit/symbol-graph.test.js
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-map.test.js tests/unit/agent-skill-registry.test.js
npm test
npm run lint
git diff --check
PORT=52021 HOME=/tmp/xikelab-ast-home npm start
HOME=/tmp/xikelab-ast-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52021 npm run test:e2e
```

结果记录：

- AST/Code Context 相关定向单测：5 个文件通过，16 个测试通过
- 全量单测：53 个文件通过，267 个测试通过
- lint：通过
- diff check：通过
- e2e：55/55 通过
- 工程地图脚本验证：扫描 260 个文件，8 个 focus files，evidence parserCounts 为 `acorn:7`、`heading-regex:1`；Symbol Graph 输出 80 defs / 201 refs / 54 calls / 23 route uses
- in-app Browser 复查通过：Agent 图谱工程地图扫描 260 个文件，选出 24 个 focus files；Code Evidence 显示 `257 symbols / 95 anchors / 89 imports / 1597 refs / acorn:22 / heading-regex:2`，prompt 含 `parsers acorn` 与 `Symbol graph:`。截图：`output/playwright/xikelab-ast-symbol-graph-browser.png`
- 依赖变化：新增 runtime dependency `acorn@^8.16.0`

当前未提交增量最近一次验证：

```bash
node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseMap.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/AgentSkillRegistry.js && node --check src/server/routes/agentRegistry.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/symbol-graph.test.js tests/unit/codebase-map.test.js tests/unit/code-context-evidence.test.js tests/unit/code-context-signals.test.js tests/unit/agent-skill-registry.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/metrics-store-agent-diagnostics.test.js
npm test
npm run lint
git diff --check
HOME=/tmp/xikelab-symbol-graph-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52020 npm run test:e2e
```

结果记录：

- Agent/Code Context 相关单测：7 个文件通过，26 个测试通过
- 全量单测：52 个文件通过，265 个测试通过
- lint：通过
- diff check：通过
- e2e：55/55 通过
- in-app Browser 可视复查通过：Agent 图谱工程地图扫描 259 个文件，选出 24 个 focus files，展示 305 symbols / 93 anchors；Symbol Graph 显示 80 defs / 240 refs / 32 calls / 80 route uses；`Codebase Map`、`Symbol Graph`、`Code Context`、`Code Evidence` 和 prompt `Symbol graph:` / `Code evidence:` 均正常。截图：`output/playwright/xikelab-symbol-graph-browser.png`

本轮 P0 Agent Run 会话化新增验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check src/storage/SqliteStore.js && node --check src/metrics/MetricsStore.js && node --check server.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/metrics-store-agent-diagnostics.test.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/delegations-routes.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/activity-log.test.js
npm test -- tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/agent-run-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/dispatcher/dispatcher-construction.test.js
npm test -- tests/unit/budget-policy-store.test.js tests/unit/delegation-store.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/agent-run-store.test.js tests/unit/room-adapter-budget.test.js tests/unit/metrics-store-agent-diagnostics.test.js
npm test
npm run lint
git diff --check
```

结果记录：

- 新增 Agent Run 定向单测：5 个文件通过，11 个测试通过（含 run export、Activity 关联、delegation autostart 映射）
- Agent Run 生命周期定向单测：5 个文件通过，19 个测试通过（含 RoomAdapter success / budget deferred / metrics 复用）
- Agent Run 治理链路定向单测：7 个文件通过，18 个测试通过（含 budget incident、delegation payload、approval/job payload、autostart run transition）
- 全量单测：56 个文件通过，272 个测试通过
- lint：通过
- diff check：通过

当前 P0 Agent Run governance lineage + Runs tab 筛选未提交增量验证：

```bash
node --check src/audit/ActivityLog.js && node --check src/server/routes/activity.js && node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/activity-log.test.js && node --check tests/unit/routes/activity-routes.test.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js
npm test
npm run lint
git diff --check
npm run test:e2e
```

结果记录：

- Agent Run / Activity 定向测试：4 个文件通过，14 个测试通过。
- Agent Run / 治理链路相关测试：10 个文件通过，29 个测试通过。
- 全量单元测试：61 个文件通过，309 个测试通过。
- lint / diff check：通过。
- e2e：72/72 通过，服务已停止。
- 新增回归覆盖 `governanceLineage`、budget blocker、autopilot job 关联、Markdown Governance Lineage 与 Execution Archives 导出、`deferReason` / `hasGovernance` / `budgetIncidentId` route 查询过滤、安全 replay plan 记录、replay result 归档、执行后 archive artifact、Idea-to-Archive Run Draft、intake archive、Complete Idea Run、verification tool result 与 final archive、Activity `agentRunId` 过滤与反向打开 Run，以及历史 `budgetIncidentId` 不应在非 open / 非 deferred 状态下误判为 blocker。

当前 P0 Idea-to-Archive 自动验证执行器未提交增量验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-idea-auto-home PORT=52043 npm start
HOME=/tmp/xikelab-idea-auto-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52043 npm run test:e2e
```

结果记录：

- Agent Run / Activity / 自动验证定向测试：5 个文件通过，17 个测试通过。
- Agent Run / 治理链路相关测试：12 个文件通过，40 个测试通过。
- 全量单元测试：61 个文件通过，309 个测试通过。
- lint：通过。
- e2e：72/72 通过，服务已停止；覆盖 `Auto Verify & Archive`、自动验证完成、`Auto verification passed`、verification tool result 与 final archive。
- e2e 清理：删除 2 个测试 run、8 条 message、1 条 tool result、18 条 event。
- 新增回归覆盖验证命令解析、allowlist、`PermissionGovernance` 判定、安全 `node --check` 执行、非 allowlist 危险命令执行前阻断、blocked/approval-required evidence、`POST /api/agent-runs/:id/idea-auto-execute` route 与 Runs tab 自动归档入口。

当前 P0 Idea-to-Archive work evidence + 自动验证归档未提交增量验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/permission-governance.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-work-evidence-home PORT=52044 npm start
HOME=/tmp/xikelab-work-evidence-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52044 npm run test:e2e
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh open 'http://127.0.0.1:52044/?t=...'
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh snapshot
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh screenshot --filename output/playwright/xikelab-idea-work-evidence.png --full-page
```

结果记录：

- 新增 executor 单测：1 个文件通过，4 个测试通过。
- Agent Run / Activity / 权限治理定向测试：6 个文件通过，26 个测试通过。
- Agent Run / 治理链路相关测试：12 个文件通过，41 个测试通过。
- 全量单元测试：61 个文件通过，310 个测试通过。
- lint：通过。
- e2e：73/73 通过，服务已停止；新增覆盖 `Auto Work + Verify`、`Idea work plan prepared`、`git status --short`、`git diff --name-only`、`git diff --check`、verification tool result 与 final archive。
- Playwright CLI 可视复查通过：Runs tab 的 succeeded `idea_to_archive` detail 中显示 `work_plan` message、work evidence tool results、verification results 和 Execution Archive；截图：`output/playwright/xikelab-idea-work-evidence.png`。
- 新增回归覆盖 `validateWorkEvidenceCommand()`、只读 git work evidence allowlist、`work_plan` message、work evidence tool result、final archive external evidence 中的 `workPlan` / `workEvidence`，以及非 allowlist 命令执行前阻断。

当前 P0 Idea-to-Archive 受治理文件改动 + 截图证据挂载未提交增量验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/permission-governance.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
git diff --check
HOME=/tmp/xikelab-file-change-home-2 PORT=52048 npm start
HOME=/tmp/xikelab-file-change-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52048 npm run test:e2e
```

结果记录：

- 新增 executor 单测：1 个文件通过，5 个测试通过。
- Agent Run / Activity / 权限治理定向测试：6 个文件通过，27 个测试通过。
- Agent Run / 治理链路相关测试：12 个文件通过，42 个测试通过。
- 全量单元测试：61 个文件通过，311 个测试通过。
- lint / diff check：通过。
- e2e：75/75 通过，服务已停止；新增覆盖受治理 `file.write output/playwright/e2e-idea-work-generated.js`、`file changes 1`、`artifacts 1`、截图 evidence exists、`node --check output/playwright/e2e-idea-work-generated.js` 与 final archive。
- 新增回归覆盖 `validateFileChange()`、safe project file allowlist、外部路径/`.env` 阻断、`file.write` permission decision、before/after sha256、artifact metadata、文件改动 tool result 与 archive external evidence。

当前 P0 Idea-to-Archive UI manifest editor 未提交增量验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm run lint
git diff --check
npm test
HOME=/tmp/xikelab-ui-manifest-home PORT=52049 npm start
HOME=/tmp/xikelab-ui-manifest-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52049 npm run test:e2e
```

结果记录：

- executor 单测：1 个文件通过，5 个测试通过。
- 全量单测：61 个文件通过，311 个测试通过。
- lint / diff check：通过。
- e2e：76/76 通过，服务已停止；新增覆盖 `Edit Manifest` UI、JSON manifest 提交、受治理 `file.write`、自定义 `node --check`、`file changes 1` 与 `artifacts 1`。
- in-app Browser 复查通过：queued `idea_to_archive` detail 同时显示 `Auto Work + Verify` 与 `Edit Manifest`；截图：`output/playwright/xikelab-ui-manifest-editor-browser.png`。

当前 P0 Idea-to-Archive approval resume file change 未提交增量验证：

```bash
node --check src/permissions/PermissionGovernance.js && node --check src/agents/AgentRunVerificationExecutor.js && node --check public/app.js && node --check tests/unit/permission-governance.test.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
git diff --check
npm test -- tests/unit/permission-governance.test.js tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-approval-resume-home-2 PORT=52052 npm start
HOME=/tmp/xikelab-approval-resume-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52052 npm run test:e2e
```

结果记录：

- 权限治理 + executor 定向测试：2 个测试文件通过，15 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，44 个测试通过。
- 全量单测：61 个文件通过，313 个测试通过。
- lint / diff check：通过。
- e2e：77/77 通过，服务已停止；新增覆盖 `requiresApproval` manifest 首次执行转 `deferred`、审批通过、带同一 `approvalId` 续跑、`file.write` 成功、`file changes 1` 进入 final archive。
- in-app Browser 复查尝试时 Browser 插件 `iab` 当前不可用；该项未作为阻塞，因同一浏览器交互链路已由 Playwright e2e 实机通过。

当前 P0 Idea-to-Archive expanded command policy 未提交增量验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-command-policy-home PORT=52054 npm start
HOME=/tmp/xikelab-command-policy-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52054 npm run test:e2e
```

结果记录：

- executor 单测：1 个文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个文件通过，45 个测试通过。
- 全量单测：61 个文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：78/78 通过，服务已停止；新增覆盖 generated `node:test` 文件、`node --test output/playwright/e2e-node-policy-*.mjs`、`git status --porcelain=v1`、`git diff --stat`、受治理 `file.write` 与 final archive。
- 新增回归覆盖 exact allowlisted `npm run`、project-local `node --test`、selected `node scripts/*`、只读 git work evidence 扩展，以及外部路径/未知脚本/非 allowlist git 命令执行前阻断。

当前 P0 Idea-to-Archive generated work manifest fileChanges 未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-work-manifest-home PORT=52056 npm start
HOME=/tmp/xikelab-work-manifest-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52056 npm run test:e2e
```

结果记录：

- Agent Run store/route 定向测试：2 个文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个文件通过，45 个测试通过。
- 全量单测：61 个文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：80/80 通过，服务已停止且 52056 无 LISTEN 进程；新增覆盖 `Generate Manifest`、`manifest_draft` message、`agent.run.idea_manifest_drafted` Activity、`Edit Manifest` 预填带 `fileChanges` 的 generated work manifest、受治理 `file.write output/playwright/idea-work-agent-run-e2e-generated-manifest-*.md`、`file changes 1`、继续执行 `node --check public/app.js`、只读 work evidence 和 Execution Archive。
- 新增回归覆盖 draft 生成不写盘、不执行命令；生成的 manifest 默认带工作清单 artifact，真正执行仍走 `idea-auto-execute` 的 allowlist、PermissionGovernance、work evidence、verification 和 archive 链路。

当前 P0 Idea-to-Archive 本地 Agent file-change plan 未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-agent-manifest-home-3 PORT=52059 npm start
HOME=/tmp/xikelab-agent-manifest-home-3 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52059 npm run test:e2e
```

结果记录：

- Agent Run store/route 定向测试：2 个文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个文件通过，45 个测试通过。
- 全量单测：61 个文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：80/80 通过，服务已停止且 52059 无 LISTEN 进程；新增覆盖 generated manifest 预填包含 `output/playwright/idea-agent-change-*.js`、`local-agent-filechange-synthesizer`、Agent JS 计划的 `node --check`，执行后 final archive 显示 `file changes 2`，并记录两个受治理 `file.write`。
- 账号/密钥/外部授权：本阶段未接入外部付费模型；采用本地 deterministic Agent synthesizer，避免新增账号、API Key、登录、额度或付款风险。

当前 P0 Idea-to-Archive 可选模型 adapter source patch manifest 未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-model-patch-home-2 PORT=52061 npm start
HOME=/tmp/xikelab-model-patch-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52061 npm run test:e2e
```

结果记录：

- Agent Run store/route 定向测试：2 个文件通过，8 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个文件通过，46 个测试通过。
- 全量单测：61 个文件通过，315 个测试通过。
- lint / diff check：通过。
- e2e：81/81 通过，服务已停止且 52061 无 LISTEN 进程；新增覆盖 `Generate Patch`、`idea_patch_manifest_draft`、默认 `useModel:false` 本地 fallback、`Edit Manifest` 预填 append 到 `public/app.js` 的 source patch proposal、`node --check public/app.js`、`git status --porcelain=v1` 与 `git diff --stat`。
- 账号/密钥/外部授权：UI 默认不调用外部模型，不新增账号/API Key/登录/额度风险；当前环境只看到 `CLAUDE_BIN_PATH` 与 Anthropic base URL 备份/主变量，未看到 `OPENAI_API_KEY`。API 可显式 `useModel:true` 使用现有 adapter，adapter 不可用、调用失败或返回 JSON 不安全时记录 generation error 并降级到本地 fallback。

当前 P0 Governance Center 最小统一入口 + 预算处理动作未提交增量验证：

```bash
node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/routes/governance-routes.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/routes/governance-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-center-actions-home-2 PORT=52065 npm start
HOME=/tmp/xikelab-governance-center-actions-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52065 npm run test:e2e
```

结果记录：

- Governance route 定向测试：1 个文件通过，2 个测试通过。
- Governance / Activity / Agent Run 定向测试：5 个文件通过，17 个测试通过。
- Agent Run / Activity / 权限治理相关测试：13 个文件通过，48 个测试通过。
- 全量单元测试：61 个文件通过，315 个测试通过。
- lint / diff check：通过。
- e2e：87/87 通过，服务已停止且 52065 无 LISTEN 进程；新增覆盖侧边栏 `btnGovernance`、Governance Center modal、KPI、Next Actions、Open Items、Agent Runs、Recent Activity、刷新按钮，以及真实创建 warning budget incident 后在 Governance Center 直接 `标记已处理` 并刷新消失。
- 可视复查：Governance Center 截图保存到 `output/playwright/xikelab-governance-center.png`，52063 临时服务已停止且无 LISTEN 进程。

当前 P0 Agent Run session-level 聚合未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-verification-executor.test.js
npm test
npm run lint
HOME=/tmp/xikelab-agent-run-session-home-2 PORT=52067 npm start
HOME=/tmp/xikelab-agent-run-session-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52067 npm run test:e2e
```

结果记录：

- Agent Run store / route 定向测试：2 个文件通过，9 个测试通过。
- Agent Run / Governance / Activity 相关测试：6 个文件通过，25 个测试通过。
- 全量单元测试：61 个文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：88/88 通过，服务已停止且 52066 / 52067 无 LISTEN 进程；新增覆盖 Runs tab `sessionId` 筛选、`Session Timeline`、同一 session 的 sibling run 聚合、Activity 反向打开 run 后展示 session-level counts。

当前 P0 Governance Center 审批续跑入口未提交增量验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/server/routes/agentRuns.js && node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-approval-resume-home-3 PORT=52070 npm start
HOME=/tmp/xikelab-governance-approval-resume-home-3 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52070 npm run test:e2e
```

结果记录：

- Agent Run / Governance / Permission / Activity 定向测试：7 个文件通过，34 个测试通过。
- 全量单测：61 个文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：90/90 通过，服务已停止且 52070 无 LISTEN 进程；新增覆盖 Governance Center `Approval Actions`、deferred `idea_to_archive` 审批项展示、`批准并续跑`、同一 approvalId + 同一 manifest 的安全恢复写入、`file.write`、`node --check` 和 Execution Archive。
- 新增回归覆盖 pending approval resume manifest 入 run timeline、`POST /api/agent-runs/:id/approval-resume` 只接受已 approved 的原审批、Governance summary 标记 `canApproveResume`，以及前端按钮先批准再续跑。

当前 P0 Governance Center 执行前 diff 审查未提交增量验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/server/routes/agentRuns.js && node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-preflight-review-home PORT=52071 npm start
HOME=/tmp/xikelab-governance-preflight-review-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52071 npm run test:e2e
```

结果记录：

- Agent Run / Governance / Permission / Activity 定向测试：7 个文件通过，34 个测试通过。
- 全量单测：61 个文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：91/91 通过，服务已停止且 52071 无 LISTEN 进程；新增覆盖 Governance Center `Preflight Review`、待写文件路径、新增内容 diff preview、验证命令、随后 `批准并续跑` 和 Execution Archive。
- 新增回归覆盖 `AgentRunApprovalResumeReview` 只读生成审查摘要、`GET /api/agent-runs/:id/approval-resume-preview`、Governance summary 下发 `resumeReview`，以及前端在审批前展示 diff/command 证据。

当前 P0 模型 adapter source patch 质量评估未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-patch-quality-home-2 PORT=52073 npm start
HOME=/tmp/xikelab-patch-quality-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52073 npm run test:e2e
```

结果记录：

- Agent Run store/route 定向测试：2 个文件通过，9 个测试通过。
- Agent Run / Governance / Permission / Activity 定向测试：7 个文件通过，34 个测试通过。
- 全量单测：61 个文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：92/92 通过，服务已停止且 52073 无 LISTEN 进程；新增覆盖 `Generate Patch` 后 Run detail 中出现 `Patch quality <grade> <score>/100` 与 `proposal_only_patch` finding，再继续验证 source patch manifest 预填。
- 新增回归覆盖 `patchQuality` 分数、等级、findings/blockers、Activity 详情，以及 source patch draft 对 affected files、验证命令和 work evidence 的本地质量评估。

本轮 P0 Codebase Index 二期后端新增验证：

```bash
node --check src/agents/CodebaseQueryEngine.js && node --check src/agents/CodebaseMap.js && node --check src/agents/CodebaseIndexStore.js && node --check src/server/routes/codebaseIndex.js
npm test -- tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/unit/codebase-map.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js
node --input-type=module # 对 5 个验收问题调用 CodebaseIndexStore.query()
npm test
npm run lint
git diff --check
```

结果记录：

- Codebase Index 定向单测：5 个文件通过，7 个测试通过。
- 新增回归测试覆盖中文查询扩展、实现文件优先、超过旧 220KB 上限的 `public/app.js` handler 命中。
- 5 问 smoke：`RoomAdapter 在哪里处理预算？` 命中 `src/room/RoomAdapter.js:56`；`Agent Skill 诊断哪里写入 Activity？` 命中 `src/metrics/MetricsStore.js:103` 与 `src/audit/ActivityLog.js`；`Delegation autostart 链路？` 命中 `src/autopilot/DelegationAutostart.js`；`Agent 图谱入口 DOM handler` 命中 `public/app.js:7250`；`SymbolGraph 如何关联 route？` 命中 `src/agents/SymbolGraph.js:217`。
- 全量单测：58 个文件通过，275 个测试通过。
- lint：通过。
- diff check：通过。

当前 P0 Codebase Index per-file evidence cache + SQLite FTS/BM25 + hash-vector + TS/成员语义引用 + persistent snapshot + citation chain 未提交增量验证：

```bash
node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseMap.js && node --check src/agents/CodebaseFtsIndex.js && node --check src/agents/CodebaseVectorIndex.js && node --check src/agents/CodebasePersistentIndex.js && node --check src/agents/CodebaseCitationChain.js && node --check src/agents/CodebaseIndexStore.js
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-map.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm test
npm run lint
git diff --check
npm run test:e2e
```

结果记录：

- 新增回归测试覆盖同一 cwd 连续 rebuild 复用未变化文件 evidence、变更文件触发 stale 后重算、SQLite FTS5 / BM25 查询合并、本地 hash-vector/cosine 查询融合、query cache 上限、TSX AST、type import 跳过运行时引用、default/barrel/re-export/renamed import 绑定、TS assertion/satisfies/instantiation、method/member reference kind 进入 Symbol Graph、SQLite snapshot 冷启动查询、citation chain、每 cwd 快照保留上限。
- Codebase Index / Code Context / Symbol Graph 定向测试：6 个文件通过，22 个测试通过。
- 全量单元测试：61 个文件通过，309 个测试通过；lint 与 diff check 通过。
- e2e：启动 `npm start` 后 72/72 通过，服务已停止；Codebase Center UI 状态验证包含 vector 索引计数，Agent Run UI 状态验证包含 Archive Run 按钮、Execution Archive 视图、Idea-to-Archive Run Draft、Auto Verify & Archive 和 final archive。

当前 P0 Codebase Index code question UI 未提交增量验证：

```bash
node --check src/agents/CodebaseQuestionAnswer.js src/agents/CodebaseIndexStore.js src/server/routes/codebaseIndex.js public/app.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm test
npm run lint
HOME=/tmp/xikelab-code-question-1779744824 PORT=52076 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-code-question-1779744824 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52076 npm run test:e2e
```

结果记录：

- Codebase Index store/route 定向测试：2 个文件通过，11 个测试通过。
- 全量单元测试：61 个文件通过，317 个测试通过。
- lint / diff check：通过。
- e2e：93/93 通过，服务已停止且 52076 无 LISTEN 进程；新增覆盖 Codebase Center `Answer`、`Local Code Answer`、`C1` citation、`public/app.js` 命中，以及原有“添加结果到 Dispatch Preview”链路。
- 新增能力：`CodebaseQuestionAnswer` 复用 query/FTS/vector/citation 结果，生成 deterministic answer、answerLines、citations、coverage 和 limitations；UI 只展示本地证据汇总，不调用模型、不上传代码、不替代 path/line/reason/citation。

当前 Codebase Answer -> Dispatch / Agent Run 证据链未提交增量验证：

```bash
node --check src/agents/AgentSkillRegistry.js
node --check src/server/routes/agentRegistry.js
node --check src/server/routes/agentRuns.js
node --check src/agents/AgentRunStore.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/routes/agent-runs-routes.test.js
git diff --check
npm test
npm run lint
HOME=/tmp/xikelab-code-answer-dispatch-1779745702b PORT=52078 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-code-answer-dispatch-1779745702b PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52078 npm run test:e2e
```

结果记录：

- Agent Run / Agent Registry 定向测试：3 个文件通过，18 个测试通过。
- 全量单元测试：61 个文件通过，317 个测试通过；lint 与 diff check 通过。
- e2e：96/96 通过，服务已停止且 52078 无 LISTEN 进程；新增覆盖 Codebase Answer 添加到 Dispatch Preview、Dispatch prompt preview 展示 `Code Question Answer` / `C1`、从 Dispatch 创建 Idea Run 后 Run detail 中可见同一 code answer 证据。
- 新增能力：`codebaseQuestionAnswer` 现在会进入 Dispatch Preview state、`/api/agent-registry/classify` prompt preview、`AgentRunStore.createIdeaRun()` details / decision / summary / intake archive，并在 `buildPatchPrompt()` 中作为 source patch prompt 的本地证据输入；仍只保存 path/line/citation 摘要，不执行命令、不上传代码、不调用模型。

当前 Approval Resume Gate 审计查询未提交增量验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js
node --check src/agents/AgentRunStore.js
node --check src/agents/AgentRunVerificationExecutor.js
node --check src/server/routes/agentRuns.js
node --check src/server/routes/governance.js
node --check public/app.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/agent-run-verification-executor.test.js
git diff --check
npm test
npm run lint
HOME=/tmp/xikelab-gate-audit-1779746429 PORT=52079 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-gate-audit-1779746429 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52079 npm run test:e2e
```

结果记录：

- Agent Run / Governance / Executor 定向测试：4 个文件通过，19 个测试通过。
- 全量单元测试：61 个文件通过，318 个测试通过；lint 与 diff check 通过。
- e2e：97/97 通过，服务已停止且 52079 无 LISTEN 进程；新增覆盖 approval resume 成功后 Run detail 显示 `Approval Resume Gate`、`review-*`、审批恢复生成文件路径和 `node --check ...` 命令。
- 新增能力：`buildApprovalResumeGateAudit()` 生成可归档 gate audit；`approval-resume-preview` 和 Governance summary 返回 preview audit；`approval-resume` 成功前调用 `recordApprovalResumeGateAudit()` 写入 run details、decision message 和 `agent.run.approval_resume_gate_accepted` Activity；最终 `idea-auto-execute` archive evidence 也携带 `resumeReviewGate` / `resumeReviewGateAudit`。

当前 Agent Runs gate audit 过滤未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
HOME=/tmp/xikelab-gate-filter-1779747110 PORT=52080 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-gate-filter-1779747110 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52080 npm run test:e2e
```

结果记录：

- Agent Run store/route 定向测试：2 个文件通过，10 个测试通过。
- 全量单元测试：61 个文件通过，318 个测试通过；lint 与 diff check 通过。
- e2e：98/98 通过，服务已停止且 52080 无 LISTEN 进程；新增覆盖在 Run detail 拿到 `review-*` 后，用 Runs tab 的 `reviewGateId` 筛选反查到对应 approval resume run。
- 新增能力：`AgentRunStore.list()` 支持 `approvalResumeGateId` / `reviewGateId` 和 `approvalResumeGateSha256` / `reviewSha256`，从 details JSON 中匹配 `approvalResumeGateId`、`approvalResumeGateAudit.id`、`approvalResumeGateSha256` 或 `approvalResumeGateAudit.sha256`；`parseListQuery()` 和 Runs tab 对应新增 gate id/hash 筛选输入。

当前 Activity gate audit 深链未提交增量验证：

```bash
node --check src/audit/ActivityLog.js
node --check src/server/routes/activity.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-activity-gate-rerun-1779747710 PORT=52082 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52082/?t=...' npm run test:e2e
```

结果记录：

- Activity Log / route 定向测试：2 个文件通过，8 个测试通过。
- 全量单元测试：61 个文件通过，319 个测试通过；lint 与 diff check 通过。
- e2e：100/100 通过，服务已停止且 52082 无 LISTEN 进程；新增覆盖用 `reviewGateId` 打开 Activity Center、列表命中 `agent.run.approval_resume_gate_accepted`、详情显示 `Approval Resume Gate` 面板，并从 Activity gate audit 事件直接打开对应 approval resume Agent Run。
- 新增能力：`ActivityLog.list()` 与 `/api/activity` 支持 `approvalResumeGateId` / `reviewGateId` 和 `approvalResumeGateSha256` / `reviewSha256` 过滤；Activity Center 增加 `reviewGateId` / `reviewSha256` 输入、gate hint、`Approval Resume Gate` 详情面板、gate counts/files/commands 摘要和 Run 反跳按钮。

当前 Gate audit report 对账导出未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-gate-report-1779748534 PORT=52083 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52083/?t=...' npm run test:e2e
```

结果记录：

- Agent Run / Activity 定向测试：4 个文件通过，18 个测试通过。
- 全量单元测试：61 个文件通过，319 个测试通过；lint 与 diff check 通过。
- e2e：101/101 通过，服务已停止且 52083 无 LISTEN 进程；新增覆盖从 approval resume Agent Run 打开 `Gate Audit Report`，报告包含 gate id、`Verified: yes`、`agent.run.approval_resume_gate_accepted` Activity source 和 archive source。
- 新增能力：`AgentRunStore.getApprovalResumeGateAuditReport()` 会把 run details、decision message、Activity、archive evidence 中的 approval resume gate audit 归一化、生成 digest、列出 checks/sources/mismatches，并在四类来源一致时标记 `verified=true`；`GET /api/agent-runs/:id/approval-resume-gate-audit?format=json|markdown` 暴露同一报告；`exportRun()` 的 JSON/Markdown 导出也带 gate audit report 摘要；Run detail 的 `Approval Resume Gate` 区块新增 `Gate Audit Report` 按钮。

当前 Gate audit report 文件落盘归档未提交增量验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-gate-report-artifact-1779748958 PORT=52084 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52084/?t=...' npm run test:e2e
```

结果记录：

- Agent Run / Activity 定向测试：4 个文件通过，18 个测试通过。
- 全量单元测试：61 个文件通过，319 个测试通过；lint 与 diff check 通过。
- e2e：102/102 通过，服务已停止且 52084 无 LISTEN 进程；新增覆盖点击 `Archive Report` 后，Run detail 的 Execution Archive 显示 `Gate audit report archived` 和 `output/playwright/gate-audit-reports/agent-run-e2e-approval-resume-*.md`。
- 新增能力：`AgentRunStore.recordApprovalResumeGateAuditReportArtifact()` 将 gate audit report markdown 写入 `output/playwright/gate-audit-reports/`，记录 artifact path/size/sha256/reportId/gateId/verified，并调用 `recordArchive()` 把 `gateAuditReportArtifact` 和 `resumeReviewGateAudit` 写入 archive evidence；`POST /api/agent-runs/:id/approval-resume-gate-audit/archive` 暴露该能力；Run detail 的 `Approval Resume Gate` 区块新增 `Archive Report` 按钮。

本轮 Agent Center 最小 UI 新增验证：

```bash
node --check public/app.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-skill-registry.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
HOME=/tmp/xikelab-agent-center-home PORT=52031 npm start
HOME=/tmp/xikelab-agent-center-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52031 npm run test:e2e
```

结果记录：

- 前端语法检查：通过。
- Agent/Run/Activity 定向单测：6 个文件通过，29 个测试通过。
- e2e：58/58 通过，新增覆盖 Agent Center tabs、Runs tab 控件和 Policies tab 编辑器。
- UI 文档同步后全量单测：58 个文件通过，275 个测试通过；lint：通过；diff check：通过。

本轮 Codebase Center 最小 UI 新增验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/unit/agent-skill-registry.test.js tests/unit/routes/agent-registry-routes.test.js
HOME=/tmp/xikelab-codebase-center-home PORT=52032 npm start
HOME=/tmp/xikelab-codebase-center-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52032 npm run test:e2e
node --input-type=module # Playwright 截图复查 Codebase Center 查询结果
```

结果记录：

- 前端与 e2e 脚本语法检查：通过。
- Codebase/Agent Registry 定向单测：4 个文件通过，22 个测试通过。
- Codebase Index route 修复：未显式传 cwd 时使用 `process.cwd()`，避免隔离 `HOME=/tmp` 的 e2e 环境把当前项目路径误判为沙箱外；新增 route 回归测试。
- e2e：63/63 通过，新增覆盖 `btnCodebaseCenter`、Codebase Center modal、query results、结果添加到 Dispatch Preview。
- 截图复查：`output/playwright/xikelab-codebase-center.png`。
- 文档同步后全量单测：58 个文件通过，276 个测试通过；lint：通过；diff check：通过。

本轮权限治理扩展新增验证：

```bash
node --check server.js && node --check src/permissions/PermissionGovernance.js && node --check src/server/routes/roomAdapters.js && node --check src/server/routes/watcher.js && node --check src/server/routes/webhook.js && node --check src/server/routes/mcp.js
npm test -- tests/unit/permission-governance.test.js tests/unit/routes/permission-governance-routes.test.js tests/unit/command-approval-gate.test.js
npm test
npm run lint
git diff --check
```

结果记录：

- 权限治理核心单测：覆盖危险 shell、外部/敏感文件、敏感目录、私网/公网 network upload、plugin execute、provider config、auto-accept scope。
- 路由集成单测：覆盖 Room Adapter/Watcher 配置、Watcher test、Webhook create/test、MCP create/list tools 的审批前不落盘、不执行、不联网、不拉起子进程。
- 原有危险命令审批测试保持通过，避免破坏 TerminalApprovalGate / dangerous command approval 行为。
- 文档同步后全量单测：60 个文件通过，285 个测试通过；lint：通过；diff check：通过。

---

本轮提交前已验证：

```bash
npm test
npm run lint
git diff --cached --check
HOME=/tmp/xikelab-checkpoint-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52001 npm run test:e2e
```

结果记录：

- 单元测试：44 个文件通过，228 个测试通过
- lint：通过
- staged diff check：通过
- e2e：47/47 通过

注意：当时因为 Node 26 原生绑定缺失，执行过：

```bash
npm rebuild better-sqlite3
```

如果下个对话框遇到 `better-sqlite3` native binding 报错，优先重新执行上面命令。

## 前端烟测截图

历史烟测截图位置：

- `/tmp/xikelab-delegation-autostart-ui.png`
- `/tmp/xikelab-delegation-autostart-approval-ui.png`

这两个截图用于证明：

- Delegation Center 中出现 `审批后自启动`
- 点击后能创建 pending approval 并打开 Approval Center

## Crow5 学习研究进度与后续方向

当前这条线不是“复刻 Crow5 UI”，而是研究 Crow5 的技术精髓，然后把适合 Xike Lab 的部分吸收为本地、可审计、可测试的能力。按 `docs/crow5-deep-analysis-personal-growth-plan.md` 的框架估算：

- 多 Agent / Skill Registry：约 45% 完成。已落地 profile、dispatch tags、Skill 绑定、诊断、prompt runtime context、profile governance；还缺子 Agent run/session 化、真实并发任务状态、主 Agent 汇总结论视图。
- 本地治理 / 审批 / 审计 / 预算：约 99% 完成。Paperclip 风格的预算、审批、审计、委派、自启动已经可运行；Agent Run 已有 governance lineage、Runs tab 治理筛选、安全 replay plan、replay result 归档、执行后 archive artifact、Execution Archive 视图、Activity 反向跳转、Governance Center、session-level 聚合、Session Timeline、Session Evidence markdown UI 导出与文件归档，以及 Idea-to-Archive intake / generated work manifest draft / 本地 Agent file-change plan / 可选模型 source patch manifest / patch quality / execution completion / work plan / 受治理文件改动 / UI manifest editor / approval resume file change / Governance Center 批准并续跑 / Preflight Review / approve-before-run diff gate / expanded command policy / work evidence / 安全自动验证 / artifact evidence / final archive；后续主要是归档 artifact 反查/下载列表、更细粒度 gate 审计对账和 Tree-sitter/LSP 级 code evidence。
- 全工程代码理解：约 67% 完成。已从路径 signals 进化到 Code Evidence、Codebase Map、Acorn/Babel AST 优先 references、Symbol Graph，并补上 rebuild 级 per-file evidence cache、内存 SQLite FTS5/BM25、本地 hash-vector/cosine 融合、TS/TSX/JSX AST、import/export 绑定、TS 类型级引用、TS assertion/satisfies/instantiation、method/member 语义引用、`type-implementation` 方法契约证据、本地 SQLite snapshot、citation chain、code question UI，以及 Codebase Answer 注入 Dispatch prompt preview / Agent Run archive；还缺真正 LSP/Tree-sitter 深层动态/跨语言类型推导和跨语言引用。
- 桌面开发体验：约 30% 完成。Xike Lab 仍是 Node/Express + Web UI + Electron packaging；Crow5 的 Tauri + sidecar + SQLite session/message/part 架构还处于研究吸收阶段，不建议马上迁移主项目。
- 模型/provider 生态：约 25% 完成。Xike Lab 有多 adapter，但还没有 Crow5 那种 provider onboarding、模型缓存、用量额度 UI、自定义模型管理和模型选择策略。
- Skill 市场/插件生态：约 35% 完成。已有本地 SkillStore、注入与诊断；还缺分类、安装来源、duplicate/rank、权限策略、usage analytics、插件 SDK 化。
- 产品主路径 Idea -> Plan -> Work -> Verify -> Archive：约 81% 完成。已能从 Dispatch Preview 把一句 idea 变成带 code context、code question citations、skills/tags、decision、summary 和 intake archive 的 `idea_to_archive` Run Draft，并通过 `Generate Manifest` 生成可审计、可编辑、默认带 `output/playwright/idea-work-*.md` 工作清单 artifact 与 `output/playwright/idea-agent-change-*.js` 本地 Agent file-change plan 的 verification/work-evidence manifest draft，通过 `Generate Patch` 生成默认本地 fallback、可选模型 adapter 的 source patch manifest draft，且带本地 patch quality 分数、finding 和 Activity 证据；通过 `Auto Work + Verify` 或 `Edit Manifest` 生成 work plan、执行受治理文件改动、采集只读 git work evidence、执行 allowlist 内本地验证命令（含 `node --test` / exact `npm run` / selected `node scripts/*`）、记录 screenshot/test artifact metadata、succeeded/failed final status 和 final archive；显式审批型文件改动可先 defer，再在 Governance Center 的 Approval Actions 中查看 Preflight Review 和 `Gate review-*`，通过 approve-before-run diff gate 校验后批准并续跑，并将 gate audit 写入 run details、decision message、Activity 和 final archive evidence；Runs tab 可按 gate id/hash 反查对应 run。

总体判断：已经完成 Crow5/Paperclip 精髓中“本地治理 + Agent/Skill 可解释调度”的第一层吸收，距离“真正超越 Crow5”还差核心三块：**可会话化的多 Agent run、真实代码库索引、Idea-to-Archive 产品主路径**。

## 下个阶段优先路线

不要重新泛泛研究，按下面顺序做，每一步都要有测试和 UI/接口证据。

### P0. Agent Run 会话化

目标：把 Crow5 的 `session/message/part/tool-output/session_diff` 思路做成 Xike Lab 自己的 `agent_runs` 抽象。

建议落地：

- 新增 `src/agents/AgentRunStore.js` 或接入现有 metrics/audit store，记录 `runId`、`roomId`、`task`、`chiefProfileId`、`subAgentProfileIds`、`status`、`startedAt/endedAt`。
- 记录每个子 Agent 的 message/tool summary、approval gate、budget preflight、result summary。
- Activity Center 反向链接到 Agent 图谱：从某次 audit event 能跳到对应 profile、skills、evidence、symbolGraph。
- UI 上先做“最近 Agent Runs”小面板，不急着重构全局页面。

验收：用户输入一个任务后，可以看到“谁被派出、为什么派出、用了哪些 Skill、做了什么、是否被审批/预算阻断”。

### P0. Codebase Index 二期

目标：从当前 AST 优先 POC 继续往 Crow5 的 codesearch / 全工程理解推进。

建议落地：

- `JavaScriptAstAnalyzer` 已补 TS/TSX/JSX 路径：使用 `@babel/parser`，不走更复杂的 regex。
- SQLite FTS5 / BM25 已补为本地全文排序层，和现有确定性 symbol/evidence 排序并行，不替代符号检索。
- `SymbolGraph` 已增加 import/export 名称绑定，处理 re-export、default export、renamed import。
- Codebase Map rebuild 级增量缓存已补：基于 mtime/size/hash，只重算变化文件；SQLite snapshot、citation chain、本地 hash-vector 融合、TS 类型级引用、method/member 语义引用、`type-implementation` 方法契约证据、code question UI 与 Agent prompt / Run archive 注入已补；后续可继续做真正动态/跨语言引用、Tree-sitter/LSP 更深类型推导，或把 question answer evidence 做成多轮 run handoff 的可查询快照。
- 新增 `/api/agent-registry/code-search` 或 `/api/codebase/search`，返回 path、line、reason、symbol/semantic score。

验收：问“RoomAdapter 在哪里接入预算？”必须返回路径、行号、命中原因，并能进入 Agent prompt。

### P0. Idea-to-Archive Flow

目标：把已有治理、委派、Autopilot、Agent 图谱包装成用户能理解的主路径。

建议落地：

- 增加一个“新任务/Idea”入口：用户一句话 -> plan preview -> 建议 Agent -> 预算/审批预估 -> 执行/委派。
- 执行后生成本地归档：任务目标、涉及文件、Agent run、预算/审批、测试结果、截图、后续行动。
- 不要把 Xike Lab 改成 SaaS，不要做企业工单系统；这是本机 AI 开发工作台的任务闭环。

验收：一个任务能从输入、分派、执行、验证、审计、归档完整走通。

### P1. Model / Skill Center

目标：学习 Crow5 的 provider/model/skill 管理体验，但做成 Xike Lab 本地版。

建议落地：

- 已完成收口：Agent Center `Models/Skills` tab 只读展示本地 provider 状态、模型候选清单、enabled skills、profile skill coverage、missing bindings、dispatch hint 缺口、No live ping 边界、Model Recommendations 推荐来源、Skill Source & Risk 来源/风险统计；不写 provider 配置、不展示密钥、不接云账号或 SaaS 租户。
- 后续 Provider registry：provider 最近使用、无副作用健康检查历史、错误摘要。
- 后续 Model picker：继续把推荐来源细化到 tag/profile/run 历史。
- 后续 Skill Center：补每次注入的 usage analytics；显式 conflict metadata 目前不从 `/api/skills` list 暴露。

验收：用户不用编辑 JSON，也能知道当前模型/Skill 是否可用，以及为什么被注入。

## 哪些可以直接复刻，哪些必须自己写

### 可以直接复刻或近似移植

前提：只针对本机已有、许可证允许或属于通用配置思想的内容；复刻时保留来源记录，不能引入 Crow5 品牌。

- Paperclip 仓库 `/Users/hxx/paperclip` 是 MIT License。其 `docs/api/approvals.md`、`docs/api/issues.md`、`docs/api/agents.md`、`docs/guides/execution-policy.md`、`doc/spec/agent-runs.md`、`doc/plans/2026-03-14-budget-policies-and-enforcement.md`、`doc/plugins/PLUGIN_SPEC.md`、`skills/*/SKILL.md` 可以直接研究结构，必要时可移植小段 schema、测试模式、脚本模式，但要保留 MIT 许可要求。
- Paperclip 的 issue/task/approval/agent-run 思路可以复刻为 Xike Lab 的本地 run/task lineage，但字段名和 UI 文案应贴合 Xike Lab，不要把产品变成 Paperclip。
- Crow5 `default-opencode.jsonc` 暴露出的“chiefAgent/defaultSubAgents/ondemandAgents/skillRegistry/agentSkillBindings/dispatchRules”结构可以复刻为架构模式。Xike Lab 已经用自己的 profile/tag/skill/governance 实现了同类原语。
- Crow5 Skill 的 `SKILL.md + frontmatter + description + body + 绑定到 Agent` 形态可以复刻。这个模式本身也是 OpenCode/Codex/Claude 生态的通用技能形态。
- Crow5 的 UI 组织思路可以复刻：输入框下方模型/Skill/权限，右侧审查/文件变化，设置页 provider/skill 状态，错误和用量可见。

### 必须自己写，不能照搬

- Crow5 app 的二进制、前端资源、品牌、角色名、图标、文案和私有实现不能直接复制。当前没有完整源码，只能从配置、日志、DB、UI 做证据化推断。
- Agent 调度 runtime 必须自己写。Xike Lab 的房间、预算、审批、Activity、Autopilot 都是自己的边界，不能硬塞 Crow5/OpenCode 的内部会话模型。
- Symbol Graph / Codebase Index 必须自己写。当前项目路径、语言范围、UI prompt 注入、审计需求和 Crow5 不同；可以学 LSP/Tree-sitter/BM25/Vector 融合思想，不能假装复制到位。
- 权限模型必须自己写。Xike Lab 已有 owner-token、approval gate、budget policy、local filesystem guard；必须统一到本机开发场景，不要照搬 Crow5 的 permission 字段就结束。
- Model/provider 管理必须自己写。Xike Lab 需要同时兼容 Claude/Codex/Gemini/MiniMax/Ollama/OpenAI-compatible、本地代理和用户机器环境。
- 产品主路径必须自己写。Xike Lab 的差异化不是“另一个 Crow5”，而是本地治理更透明、Agent 决策更可解释、代码上下文更可审计。

## 当前需要下个对话框优先做的事

推荐顺序：

1. 先确认状态：

   ```bash
   cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板
   git status --short
   git log --oneline --decorate -5
   ```

2. 如果用户要继续开发，不要重新泛泛规划，直接从 P0 里选一个最小可验证切片。当前最推荐：**Codebase Index 的真正动态/跨语言引用、多轮 run handoff evidence 快照、Tree-sitter/LSP 更深类型推导**，或继续 Agent Run 的归档 artifact 反查/下载列表、更细粒度 gate 审计对账 / Tree-sitter-LSP code evidence；Activity 反向打开具体 Run、replay 后结果归档、执行后 archive artifact、Governance Center、Agent Run session-level 聚合、Session Evidence markdown UI 导出与文件归档、Governance Center 批准并续跑、Preflight Review、approve-before-run diff gate、approval resume gate audit、gate id/hash 过滤、Idea-to-Archive intake/completion/generated work manifest draft/本地 Agent file-change plan/可选模型 source patch manifest/patch quality/work plan/受治理文件改动/UI manifest editor/approval resume file change/expanded command policy/work evidence/artifact evidence/final archive/auto verification、本地 hash-vector 融合、code question UI、code answer 注入 Dispatch prompt / Run archive、TS 类型级引用、method/member 语义引用和 `type-implementation` 方法契约证据已经补上。

3. 如果继续 Codebase Index，下一刀可以进入真正动态/跨语言引用、Tree-sitter/LSP 更深类型推导，或把 `/api/codebase-index/question` 的 answer/citations 扩展成多轮 Agent Run 可查询快照，并保持 path/line/reason/semanticScore/citation 可审计输出。

4. 如果用户只是担心误删，回答：未误删，关键文件和 commit 都在；Paperclip 本地治理功能没有删除。

5. 如果用户要求开 PR，先确认远端权限和当前分支，再执行：

   ```bash
   git status --short
   npm test
   npm run lint
   git push -u origin codex/paperclip-local-governance
   gh pr create
   ```

## 风险与边界

- 不要建议把 Xike Lab 改成企业 SaaS。
- 不要引入云端多租户、企业账号、HR/雇佣 Agent 这类 Paperclip 企业概念。
- 本项目吸收的是 Paperclip 的理念：目标追溯、预算、审批、审计、心跳调度、委派治理。
- 已落地实现必须保持本地优先、SQLite、本机项目工作流、开发者可控。
- 如果要删除 Paperclip 文案，只删文案来源，不删治理功能。
- 如果出现大脏树，先 `git status --short` 分类，不要 `git add .`，不要 reset 用户改动。

## 快速定位命令

查 Paperclip 学习记录：

```bash
rg -n "Paperclip|local governance|start_delegation|审批后自启动|DelegationAutostart|BudgetPolicyStore|ApprovalStore" docs src public tests server.js
```

查委派自启动链路：

```bash
rg -n "start_delegation|DelegationAutostart|autostart|审批后自启动" src public tests server.js
```

查治理相关路由：

```bash
rg -n "register.*Routes|governance|approvals|budgets|delegations" src/server server.js
```

## 下个对话框最短续接版

可以直接复制下面这段给下个 Codex：

```text
请接手 Xike Lab 可视化面板项目。

项目路径：/Users/hxx/Desktop/00_项目/05_Claude可视化面板
当前分支：codex/paperclip-local-governance

先执行：
cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板
pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short

然后阅读：
1. 任务交接.md
2. 上下文交接.md
3. Claude交接提示.md
4. docs/local-governance-learning-record.md
5. docs/README.md
6. docs/xikelab-agent-skill-registry.md
7. docs/crow5-deep-analysis-personal-growth-plan.md
8. docs/crow5-local-research-evidence-map.md

背景：这个分支已经完成 Paperclip 本地治理能力吸收，最新提交包括 c599d85 / 6010a23 / a9e1a52 等；未提交增量继续推进 P0 Codebase Index 二期 per-file evidence cache、SQLite FTS/BM25、本地 hash-vector、TS/TSX/JSX AST、TS 类型级引用、TS assertion/satisfies/instantiation、method/member 语义引用、SQLite snapshot、citation chain、code question UI、本地答案引用链、Codebase Answer 注入 Dispatch prompt / Agent Run archive，以及 P0 Agent Run governance lineage / Runs tab 治理筛选 / Governance Center / Approval Actions / 批准并续跑 / Preflight Review / approve-before-run diff gate / approval resume gate audit / gate id/hash 过滤 / patch quality / session-level 聚合 / 安全 replay plan / replay result / 执行后 archive artifact / `idea_to_archive` Run Draft / generated work manifest draft / 本地 Agent file-change plan / 可选模型 source patch manifest / execution completion / work plan / 受治理文件改动 / UI manifest editor / approval resume file change / expanded command policy / work evidence / artifact evidence / final archive / `idea-auto-execute` 安全自动验证执行器。已落地 Agent/Skill Registry、工程上下文分派、Code Evidence、Codebase Map、Acorn/Babel AST 优先 Symbol Graph、预算治理、审批队列、审计事件、委派中心、Autopilot start_delegation 审批后自启动。不要重新泛泛规划，不要删除这些功能，不要把 Xike Lab 改成 SaaS。先验证当前文件和测试状态，再按用户最新任务继续。

后续优先方向：当前 P0-A/P0-B/P0-C 第一批和 P1 主路径 / Model Skill Center 已做到可交接状态。下一步如果继续做功能，优先 P2 权限治理 UI 闭环；如果先稳定化，优先按交接文件建议拆分提交。不要重新泛泛规划，不要删除治理/预算/审批/审计/委派/Agent Run/Codebase Index/Agent Registry，不要把 Xike Lab 改成 SaaS。
```
