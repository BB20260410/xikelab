# Xike Lab 任务交接

更新时间：2026-05-26 10:43:33 CST

## 接手最新进展（2026-05-26 续推，工作区已 clean）

新一轮接手后已完成 P3 + P2，**下文「当前事实」里关于「有大量未提交增量」的描述已过时**（增量已全部固化为提交，工作树 clean，用 `git log` 为准）。

1. **P3 稳定化·提交拆分（完成）**：原 40+ 文件未提交增量按功能边界固化为 5 个提交，逐文件显式 stage（无 `git add .` / `reset` / 大范围 `checkout`）：
   - `Codebase Index: 接入 AST/FTS/向量/持久化快照与引用链证据后端`
   - `权限治理: 统一高风险判定与审计事件扩展`
   - `Agent Run: 会话化、审批续跑、安全自动验证与治理聚合后端`
   - `前端 UI: Agent Center / Governance Center / Codebase Center 收口`
   - `e2e 与文档: 覆盖主路径/治理/索引并更新交接`
   - 环境修复：`better-sqlite3` 需用本机 Node 22 `npm rebuild better-sqlite3`（原 binding 是 Node 24 编译，否则约 13 个 SQLite 测试报 `NODE_MODULE_VERSION` 不匹配）。

2. **P2 权限治理 UI 闭环（全部完成）**：非 Agent Run 高风险写入口的「审批后安全重试」前端闭环，6 个提交：
   - 通用机制（`public/app.js`）：`requestWithApproval`（识别 202 `approval_required` / 403 `permission_denied`）+ `approveAndRetryRequest`（批准后带 approvalId 走 `X-Panel-Approval-Id` header 重发原请求，不污染 body，累积多 id）+ `openApprovalRetryModal`（Promise 化纯弹窗，展示 action/target/risk/原因摘要）+ `handleApprovalFlow`（链式批准：重试后若再遇审批则对下一个 approval 再弹，maxSteps 防死循环）。
   - 已接入全部入口：Webhook 创建/测试（`network.upload`）、Room Adapter provider 配置（`provider.model_config.write`）、MCP server 配置/test/delete（`skill.plugin.configure`/`execute`，RCE 级）、Watcher config（双重审批 `provider.model_config.write` + `auto_accept.scope`，链式批准）。绑定原 action/target，不自动重放危险 shell。
   - 后端 `PermissionGovernance` 新增 `approvalIds` 多 id 解析与 `resolveResumeApprovalMulti`（各权限检查匹配各自 approval，无匹配保持 ask 而非 deny，仅多 id 场景；单 id 语义不变向后兼容）；`permissionApprovalIdsFromRequest` 支持 header 逗号分隔；watcher route 两检查改用多 id。
   - e2e 第 11-15 节覆盖：webhook/room-adapter/MCP create 闭环、watcher 双审批链式、MCP delete 审批弹窗。单测补多 id 链式 + MCP delete/test gate。

最新验证（2026-05-26 P2 收尾）：`npm run lint` 通过 · `npm test` 62 文件 / **329 测试** · `git diff --check` 干净 · e2e **136/136**（含 P2 全部入口闭环 + watcher 双审批链式）· 测试端口已全部停止无监听。

3. **P0-A 代码证据深化（主体完成）**：刀1 抽象 parser adapter 层（`src/agents/parsers/`，CodeContextEvidence 经 registry 取 adapter，纯重构）；刀2 analyzer 新增 `callback-registration`/`object-property-flow` 引用类型；刀3 `citationPathsFromGraph` 输出 `readable` 完整可读链（route→handler→service→test）；刀4 CodebaseQuestionAnswer 新增 `weakEvidence` 标记 + 无结构证据 limitation，守"不黑盒"红线。**待续**：刀5 前端 summary 增强（可选）、Tree-sitter 实接（接口已就位）。

4. **P9 安全审计（进行中）**：刀1 `pathInside` realpath 加固防 symlink 越界（自动覆盖 executor 文件写）；刀2 `isPrivateIp` 补 IPv4-compat IPv6 + 新增 `src/security/uploadAllowlist.js`（出站上传可选 domain allowlist，默认空=放行）+ webhook 接入。**待续**：刀3 approval bypass / artifact downloadable 审查 + SECURITY_AUDIT.md。

5. **P5 治理工作队列（完成）**：刀1 数据层（`governance_queue_items` 表 + `GovernanceQueueStore`：派生/dedupe/状态转移/分组）；刀2 `GET/POST /api/governance/queue` route（抽 collectSummary 复用，可注入 store）；刀3 Governance Center 工作队列看板（五态分栏 + 一键推进 + 并行拉取，e2e 验证 budget incident 派生为可推进队列项）。**待续**：源对象状态变化反向联动队列项（可选增强）。

最新验证（2026-05-26 P0-A 刀4 后）：`npm run lint` 通过 · `npm test` **67 文件 / 354 测试** · `git diff --check` 干净 · e2e **138/138** · 测试端口已全部停止无监听。

下一阶段（按 `docs/xikelab-remaining-roadmap-plan.md`）：P4 刀1（本地证据知识库索引）/ P6 刀1（codebase 上限集中可配置 + 扫描预算）/ P9 刀3（approval bypass 审查 + SECURITY_AUDIT.md）/ P1 Model-Skill Center 深化。

## 当前事实

- 项目路径：`/Users/hxx/Desktop/00_项目/05_Claude可视化面板`
- Git 根目录：`/Users/hxx/Desktop/00_项目/05_Claude可视化面板`
- 当前分支：`codex/paperclip-local-governance`
- 当前工作区：有未提交增量，已继续推进 P0 Codebase Index 二期 per-file evidence cache、SQLite FTS/BM25、本地 hash-vector 融合、TS/TSX/JSX AST、import/export 绑定、TS 类型级引用、TS assertion/satisfies/instantiation、method/member 语义引用、本地 SQLite snapshot 持久化与 citation chain；最新又补上 `CodebaseQuestionAnswer`、`CodebaseIndexStore.question()`、`POST /api/codebase-index/question` 和 Codebase Center `Answer`，能把 query/citation 结果压成本地 deterministic code question answer，展示 `Local Code Answer`、confidence、coverage、answer lines 和 `C1...` citations，不调用模型、不上传代码、不做黑盒总结；当前又把 `codebaseQuestionAnswer` 注入 Dispatch Preview、`/api/agent-registry/classify` 的 prompt preview、`AgentRunStore.createIdeaRun()` details/decision/summary/intake archive 与 Run detail，使 code question citations 能进入后续 Agent Run 证据链。当前新增推进 P0 Agent Run 会话化补强，补上 governance lineage、治理筛选、Runs tab 链路展示、执行后 archive artifact、Execution Archive 视图，以及从 Dispatch Preview 创建 `idea_to_archive` Run Draft、记录执行完成、写入 verification tool result 并生成最终 archive 的最小 Idea-to-Archive 闭环；最新又补上安全自动验证执行器、work evidence 采集、受治理文件改动 manifest、UI manifest editor、审批后 resume file change、expanded command policy、generated work manifest draft、本地 Agent file-change plan、可选模型 source patch manifest、Governance Center 与 Agent Run session-level 聚合。Governance Center 统一聚合审批、预算、委派、Autopilot、带治理链路的 Agent Runs 和最近治理 Activity，并提供 Next Actions 与跳转入口；Agent Run 详情可通过 `includeSession` 返回同一 `sessionId` 的 runs/messages/tool results/archives/activity/governance 聚合，Runs tab 新增 `sessionId` 筛选与 Session Timeline 反跳；Idea-to-Archive 生成 draft 默认带 `output/playwright/idea-work-*.md` 工作清单 artifact、`output/playwright/idea-agent-change-*.js` 本地 Agent 变更计划，`Generate Patch` 默认 `useModel:false` 用本地 fallback 生成 safe-root source patch proposal，API 可显式 `useModel:true` 加 `adapterId` 走现有模型 adapter，失败/不可用/不可解析时降级本地 fallback；每个 source patch manifest draft 现在会生成 `patchQuality`（分数、等级、findings、blockers、命令/evidence 覆盖），并写入 Run message 与 Activity，UI 可在执行前看到质量摘要；生成本身仍不写盘、不执行命令，真正执行继续走 `idea-auto-execute` allowlist、`PermissionGovernance`、work evidence、verification 和 final archive；显式 `requiresApproval` 的文件改动会先转 `deferred/approval_pending` 且不写盘，审批通过后带同一 `approvalId` 与同一 manifest 才恢复写入。Governance Center 现在会识别这类 deferred `idea_to_archive` run，在 Approval Actions 中展示 Preflight Review（待写文件、操作、内容 hash、diff preview、验证命令和 evidence 命令安全状态），生成 deterministic review gate（`review-*` id + sha256），再提供 `批准并续跑`；前端点击时先重新拉取 preview 确认 gate 未漂移，再批准原审批，并通过 `POST /api/agent-runs/:id/approval-resume` 携带 gate id/hash 复用 run 内保存的同一 manifest 续跑。后端会拒绝缺失 gate（428）或 stale/mismatch gate（409），执行成功响应会返回 `resumeReviewGate`；最新又把 approval resume gate audit 写入 run details、decision message、Activity 和 final archive evidence，Run detail 显示 `Approval Resume Gate`、gate id/hash、文件/命令/风险计数和涉及命令；`GET /api/agent-runs` 与 Runs tab 已支持按 `approvalResumeGateId` / `approvalResumeGateSha256` 反查对应 run；Activity Center 也已支持按同一 gate id/hash 过滤审计流，在详情中显示 `Approval Resume Gate` 面板并可从 gate audit 事件直接打开对应 Agent Run；最新再补 `GET /api/agent-runs/:id/approval-resume-gate-audit` 和 Run detail `Gate Audit Report`，把同一个 gate 在 run details、decision message、Activity、final archive evidence 四类来源中的 digest 做确定性对账，并支持 markdown 导出；当前又补 `POST /api/agent-runs/:id/approval-resume-gate-audit/archive` 与 Run detail `Archive Report`，把对账报告落盘到 `output/playwright/gate-audit-reports/*.md`，并把 report artifact 写入新的 Execution Archive evidence。
- 当前最新未提交增量（2026-05-26 07:00 CST）：P0 approve-before-run 多文件 staged diff review 已补强。`buildApprovalResumeReview()` 会对审批恢复 manifest 的每个 fileChange 计算行级 additions/removals、new/existing、扩展名、safe/blocked、approval-required、script/config/UI/large-change 等 attention flags，并汇总为 `stagedDiffReview`（含 deterministic sha256）。该摘要已进入 review gate fingerprint、`resumeReviewGateAudit`、Run detail `Approval Resume Gate`、Activity gate panel、Gate Audit Report markdown 和 report archive evidence；Governance Center Preflight Review 现在显示 `Staged Diff +N/-M`、new/existing/attention 计数与每文件 `+/-` 摘要，gate 会随 staged diff 摘要变化而变化。
- 当前最新未提交增量（2026-05-26 07:14 CST）：在多文件 staged diff review 上继续补强风险排序与命令覆盖映射。每个 fileChange 现在会得到 `commandCoverage`、`coverageStatus`、`verificationCommandCount`、`workEvidenceCommandCount`、project-wide 覆盖计数、`riskScore`、`riskLevel`、`riskRank`；`stagedDiffReview.summary` 新增 verified/uncovered/evidence-only/high-risk 计数、coverage/risk count maps 与 top risk files。coverage/risk 已进入 staged diff sha、review gate fingerprint、Governance Center Preflight Review、Run detail staged diff、Activity gate panel 和 Gate Audit Report；Gate Audit Report 对账逻辑已改为使用能跨 Activity sanitize 稳定保留的摘要字段，避免前端审计展示裁剪造成误报 mismatch。
- 当前最新未提交增量（2026-05-26 07:24 CST）：Governance Center Preflight Review 已补可操作 UI：fileChange 列表按 `riskRank` 排序，每个文件块用 `<details>` 可折叠；每个文件展示 risk rank/level/score、coverage 状态、verify/evidence 计数、后端生成的 `riskReasons`；coverage 命令以 chip 形式展示，点击会高亮对应的全局验证命令，方便从文件风险反跳到具体命令。新增 e2e `Governance Center staged diff interactions` 覆盖文件折叠、命令反跳和 risk reasons。
- 当前最新未提交增量（2026-05-26 07:33 CST）：P0 Agent Run session evidence chain 已补强。`AgentRunStore.getSessionSnapshot()` 现在返回 `evidenceChain`，按时间顺序串联同一 session 下的 run、message、tool result、archive、activity、codebase question answer 和 approval resume gate；新增 `AgentRunStore.exportSession()` 与 `/api/agent-runs/session/:sessionId?format=markdown`，Run export 也附带 session evidence 摘要；Runs tab 的 Session Timeline 显示 evidence item/kind/code answer/gate 计数和最近证据项。新增单测覆盖证据链 refs/sequence/markdown export，e2e `Agent Run session evidence chain` 覆盖真实 UI 展示。
- 当前最新未提交增量（2026-05-26 07:42 CST）：P0 command coverage 解释扩展已补强。`buildCommandCoverage()` 现在为每个 fileChange 生成 `coverageExplanations` 与 `coverageExplanationSummary`，区分 targeted verification、project-wide verification、work evidence、uncovered gap、verification missing 和 blocked file；该解释进入 staged diff sha / review gate fingerprint、Governance Center Preflight Review、Gate Audit Report markdown，但不进入跨来源 gate audit digest 的深层文件数组，避免 Activity sanitize 裁剪导致误报 mismatch。Governance Center 每个文件块新增 `Coverage explanation` 展开项，Gate Audit Report 新增 `Coverage Explanations` 计数与 per-file coverage reasons；e2e 已覆盖 UI 展开和报告导出。
- 当前最新未提交增量（2026-05-26 07:47 CST）：P0 Session Evidence markdown UI 导出入口已补上。Runs tab 的 Session Timeline 现在显示 `Export Session`，点击后调用既有 `/api/agent-runs/session/:sessionId?format=markdown`，在本地 modal 中展示完整 markdown session evidence export；e2e 已覆盖按钮存在、modal markdown 标题、`Session Evidence Chain` 和同 session run 证据内容。
- 当前最新未提交增量（2026-05-26 07:49 CST）：P0 Preflight Review coverage filter UI 已补上。Governance Center 现在通过 `renderGovernanceCoverageFilter()` 在 Preflight Review 顶部展示 `Coverage filter`，支持 `all`、`verified`、`project_wide_verified`、`evidence_only`、`uncovered`、`blocked` 六类覆盖状态筛选；每个文件块带 `data-gov-center-coverage`，空结果显示 `data-gov-center-coverage-empty`，并保留已有风险排序、折叠文件块、命令反跳和 coverage explanation。新增 e2e `Governance Center coverage filter` 覆盖切到 `uncovered` 后文件隐藏与空状态，再切回 `all`。
- 当前最新未提交增量（2026-05-26 08:14 CST）：P0 Session Evidence markdown 文件归档入口已补上。`AgentRunStore.recordSessionEvidenceArtifact()` 会把同一 session 的 markdown evidence export 写入 `output/playwright/session-evidence/agent-run-session-*.md`，计算 size/sha256，并通过既有 `recordArchive()` 挂到同 session 的 Agent Run Execution Archive；新增 `POST /api/agent-runs/session/:sessionId/archive`，Runs tab 的 Session Timeline 新增 `Archive Session`，点击后写入本地 artifact、刷新 Run detail 并在 Execution Archive 中显示 `Session evidence archived:` 与 artifact 计数。该入口校验指定 run 必须属于同一 session，写盘路径限制在 `output/playwright/session-evidence` 下。
- 当前最新未提交增量（2026-05-26 08:16 CST）：P0 Codebase Index LSP-ish 类型契约证据已补强。`JavaScriptAstAnalyzer` 会为 method / type-method 记录 `owner` 与 `ownerType`，`CodeContextEvidence` 保留该字段，`SymbolGraph` 会从 `class implements interface` 关系生成方法级 `type-implementation` 引用（例如 `WidgetController.render implements Renderable.render`）；`CodebaseCitationChain`、`CodebaseQuestionAnswer`、持久化 summary、Agent prompt summary 与前端 Symbol Graph / Local Code Answer 都会暴露 type implementation 计数。该能力不引入云索引、不调用模型，是 Tree-sitter/LSP 深化前的可审计类型契约切片。
- 当前最新未提交增量（2026-05-26 09:18 CST）：P0-A Codebase Index 三期第一刀已补强 dynamic import、route-to-test chain 和 unresolved reference summary。`JavaScriptAstAnalyzer` / `CodeContextEvidence` 会记录 `dynamic-import`；`SymbolGraph` 只把真实 route anchor 当 API route 定义，串联 route definition -> handler/service reference -> test usage，并输出 `routeTestChains`、`routeToTestChainCount`、`unresolvedReferences`、`unresolvedReferenceCount`；`CodebaseCitationChain` 会把 route-to-test chain 作为 citation path 带到 query/question 结果；`CodebaseQuestionAnswer` coverage/limitations 会明确输出证据不足、未解析引用和 route-test 链路限制，避免黑盒总结；Codebase Center / Agent preview 状态展示 parser coverage、route-test chain 和 unresolved refs，Local Code Answer 显示限制说明。
- 当前最新未提交增量（2026-05-26 09:42 CST）：P0-B Agent Run 归档 artifact 反查已完成。`AgentRunStore.getTimeline()` 现在聚合 archive evidence 中的 `evidenceArtifacts`、`sessionEvidenceArtifact`、`gateAuditReportArtifact`，返回 `artifacts` 列表，字段包含 `id/kind/path/size/sha256/sessionId/gateId/reportId/archiveId/messageId/downloadable`；新增 `listArtifacts()` 与 `readArtifact()`，只允许读取已记录且位于 `output/playwright/session-evidence/` 或 `output/playwright/gate-audit-reports/` 下的 markdown artifact，会拒绝未记录、越界/非允许目录、缺失文件和 digest mismatch；新增 `GET /api/agent-runs/:id/artifacts` 与 `GET /api/agent-runs/:id/artifacts/:artifactId/download`；Run detail 新增 `Execution Artifacts` 列表，支持复制路径和打开 markdown；`agent.run.archived` Activity 会带 artifact 摘要，Activity detail 新增 `Archive Artifacts` 反查与打开入口。e2e 已覆盖 Gate Audit Report artifact 与 Session Evidence artifact 在 Run detail / Activity 中可见并可打开。
- 当前最新未提交增量（2026-05-26 09:55 CST）：P0-C Gate 审计对账细化已完成第一刀。Gate Audit Report 保留原 canonical digest，同时新增 `file/command/risk/coverage/artifact` 分区 digest；mismatch 不再只报整体 expected/got，而是输出 `partition`、`reason`（`digest_mismatch` / `missing_field` / `unexpected_field`）、expected/actual partition digest 和缺失字段。`coverageExplanations` 仍只用于 UI/markdown 展示，不进入跨来源 coverage digest；为兼容 Activity sanitize，分区 digest 避开会被深层裁剪的 attention/top-risk 展示数组。Markdown `Sources` 会显示各 source 的分区 digest 前缀，`Mismatches` 会明确哪个 source 的哪个分区缺字段或不一致；单测覆盖 file、command、risk、coverage、artifact 五类 mismatch。
- 当前最新未提交增量（2026-05-26 10:07 CST）：P1 产品主路径完成第一刀。Agent Center Dispatch Preview 顶部新增 `Idea-to-Archive Path` 引导条，按 `Idea -> Code Context -> Dispatch Preview -> Run Draft` 显示当前状态和下一步；预演完成后会局部刷新到 `Next: 创建 Run Draft`，输入/角色/文件变化会清空旧 classification，避免 stale preview 误导。`idea_to_archive` Run detail 新增同名主路径块，把 `Idea -> Dispatch -> Manifest/Patch -> Work + Verify -> Preflight -> Archive` 串成步骤，并根据 manifest draft、patch quality、approval defer、gate audit、final archive 和 artifact 反查状态显示下一步操作。e2e 已覆盖 guided dispatch path、guided run path、final archive 和 approval resume 链路，保持现有治理/审批/gate/artifact 能力不变。
- 当前最新未提交增量（2026-05-26 10:16 CST）：P1 产品主路径第二刀完成。`idea_to_archive` Run detail 的 `Idea-to-Archive Path` 现在不只显示状态，还会渲染 `Recommended next` 主按钮和 `Other actions` 次级按钮；推荐动作按 run 状态自动指向 `Generate Manifest`、`Auto Work + Verify`、`Open Preflight Review`、`Gate Audit Report` 或 `Archive Run`。审批挂起时新增 `data-agent-run-governance-review` 入口，可从 Run detail 直接打开 Governance Center 的 Preflight Review，并高亮同 run 的审批续跑项；patch/custom manifest 仍作为次级动作保留，避免丢失手动编辑能力。e2e 已覆盖 recommended next 驱动 manifest 生成、生成后进入 auto verify、patch alternative、preflight action。
- 当前最新未提交增量（2026-05-26 10:22 CST）：P1 产品主路径第三刀完成。`idea_to_archive` 的 `Auto Work + Verify`、`Generate Manifest`、`Generate Patch`、`Run/Edit Manifest`、`Record Completion`、`Archive Run` 不再重复出现在 Run detail 顶部传统 action bar，而是集中在 `Idea-to-Archive Path` 的 `Recommended next` / `Other actions` 中；顶部 action bar 只保留审批/委派/预算、Replay、Activity 这类通用入口。非 `idea_to_archive` run 的 `Archive Run` 保持在顶部，不影响通用 Agent Run 归档。e2e 已覆盖顶部 idea action 去重、主路径保留自动验证、final archive 主路径归档动作，以及非 idea run 归档旧链路。
- 当前最新未提交增量（2026-05-26 10:27 CST）：P1 产品主路径第四刀完成。`Idea-to-Archive Path` 在 final archive 状态下新增 `Final archive` 摘要，展示归档 summary、状态、tool/file/artifact/blocker 计数；完成后推荐动作从容易误解的 `Archive Run` 改为 `Review Archive`，点击会聚焦 `Execution Archive` 区块，`Add Archive Note` 作为次级动作保留。若 run 有 artifact，则保留 `Open Artifacts` 次级入口。e2e 已覆盖 final archive 摘要、Review Archive 聚焦和既有 artifact/session/gate 链路。
- 当前最新未提交增量（2026-05-26 10:43 CST）：P1 Model / Skill Center 已收口到可交接状态。Agent Center `Models/Skills` tab 现在聚合本地 room adapter provider 状态、前端本地模型候选清单、enabled skills、profile skill coverage、missing bindings、dispatch hint 缺口、只读 provider health 边界、`Model Recommendations` 推荐来源和 `Skill Source & Risk` 来源/风险统计；UI 明确标注 `Local status only · no secrets shown · provider config is read-only here`，provider 状态显示 `No live ping`，只消费 `/api/room-adapters/providers`、`/api/skills` 和 `/api/agent-registry` 只读数据，不写 provider 配置、不展示密钥、不引入云账号或 SaaS 租户。e2e 已覆盖 Agent Center 新 tab、Provider Model Status、Model Recommendations、Skill Injection Matrix、Skill Source & Risk 和无明文 API key 展示。
- STOP 文件：不存在。
- 主线位置：`main` / `origin/main` 在 `b27f3f0 修复：Gemini 配额耗尽自动降级 + abort 不计入断路器 (#33)`。

最近关键提交：

```text
c599d85 文档: 新增任务交接入口
6010a23 侧边栏: 放大并分组功能入口
a9e1a52 权限治理: 支持审批后安全重试
ab7d007 文档: 更新 Crow5 吸收与本地治理交接
0d6f72f 权限治理: 统一判定高风险操作
f027f3a Agent 工作台 UI: 展示 Runs 与 Codebase 查询
632ad95 Agent 运行时: 接入 Registry、Run 与代码索引后端
f8b3c81 治理面板: 接入本地治理与委派自启动
```

## 接手后先执行

```bash
cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板
pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short
test ! -e STOP
git log --oneline --decorate -8
```

然后阅读：

1. `任务交接.md`
2. `上下文交接.md`
3. `Claude交接提示.md`
4. `docs/local-governance-learning-record.md`
5. `docs/README.md`
6. `docs/xikelab-agent-skill-registry.md`
7. `docs/crow5-deep-analysis-personal-growth-plan.md`
8. `docs/crow5-local-research-evidence-map.md`

## 项目定位

Xike Lab 是本地可控的多 AI Agent 开发面板，不是 SaaS。当前分支吸收 Paperclip 的本地治理思想与 Crow5 的工程上下文/Agent Registry 能力，但必须保持本机优先、SQLite、本地项目工作流、用户可控。

不要把项目改成企业多租户 SaaS；不要删除已经落地的预算、审批、审计、委派、Autopilot、Agent Run、Codebase Index、Agent/Skill Registry 功能。

## 已完成能力

- Paperclip 本地治理：预算策略、审批队列、审计事件、委派中心、Autopilot `start_delegation` 审批后自启动。
- Agent/Skill Registry：Agent Profile、Skill 绑定、dispatch tag、治理策略、诊断与 Activity 留痕。
- Agent Run 会话化：`agent_runs` / `agent_messages` / `agent_tool_results`，RoomAdapter 生命周期、预算 defer、approval/delegation/autopilot/budget Activity 聚合，JSON/Markdown export；最新未提交增量让 `/api/agent-runs/:id` 直接返回 `activityEvents`、`governanceLineage` 与 `archives`，聚合 approval / delegation / budget incident / autopilot job，给出 `nextAction` 与 blockers；新增 `AgentRunStore.getSessionSnapshot()`、`GET /api/agent-runs/session/:sessionId` 和详情 `includeSession=true`，按同一 `sessionId` 聚合 runs、messages、tool results、archives、Activity、status/source/profile counts、session-level governance blockers 与 next actions；Runs tab 支持 sessionId、sourceType、approvalId、delegationId、budgetIncidentId、deferReason、hasGovernance 筛选，并展示 Governance Chain、Session Timeline、Replay Plan、Replay Result、Archive Run 和 Execution Archive；新增安全 replay plan、replay result 与执行后 archive artifact，`POST /api/agent-runs/:id/replay-plan` 写入 `replay_plan` message 与 `agent.run.replay_planned` Activity，`POST /api/agent-runs/:id/replay-result` 写入 `replay_result` message 与 `agent.run.replay_result_recorded` Activity，`POST /api/agent-runs/:id/archive` 写入 `archive` message 与 `agent.run.archived` Activity；新增 `POST /api/agent-runs/idea`，可从 Dispatch Preview 把一句 idea 转成 queued `sourceType=idea_to_archive` Run Draft，写入 decision + summary + intake archive，并记录 `agent.run.idea_intake_created` Activity；新增 `POST /api/agent-runs/:id/idea-manifest-draft`，会基于 run 的 affected files、profile、skills 和 dispatch tags 生成不执行命令、不写盘的 `manifest_draft` message 与 `agent.run.idea_manifest_drafted` Activity，draft 默认包含 safe-root 下 `output/playwright/idea-work-*.md` 工作清单 artifact 与 `output/playwright/idea-agent-change-*.js` 本地 Agent file-change plan 两个 `fileChanges`、只读 work evidence 和匹配 affected files / Agent JS 计划的验证命令，Runs tab 的 `Generate Manifest` 会生成 draft，`Edit Manifest` 会优先用最新 draft 预填；新增 `POST /api/agent-runs/:id/idea-patch-manifest-draft` 与 Runs tab `Generate Patch`，默认本地 fallback 生成 source patch manifest，显式 `useModel:true` 可通过现有 adapter 生成真实源码补丁 proposal，adapter 失败或输出不安全时回落本地 fallback，并记录 `agent.run.idea_patch_manifest_drafted`；新增 `POST /api/agent-runs/:id/idea-execution`，可将未完成的 `idea_to_archive` draft 转入 running，记录执行摘要、verification tool result、`agent.run.idea_execution_completed` Activity，最终转为 succeeded/failed 并写入 final archive；新增 `AgentRunVerificationExecutor` 与 `POST /api/agent-runs/:id/idea-auto-execute`，只允许 safe-root 内文本文件 create/update/append、`git status --short` / `git status --porcelain=v1` / `git diff --name-only` / `git diff --stat` 等只读 work evidence、`git diff --check`、`npm test`、exact allowlisted `npm run`、`node --check`、project-local `node --test` 和 selected `node scripts/*` 安全验证命令，经 `PermissionGovernance` 的 `file.write` / `shell.exec` 判定后执行，blocked/approval-required 命令或文件改动会作为 evidence 写入 run；Runs tab 已增加 `Auto Work + Verify`、`Generate Manifest`、`Generate Patch` 和 `Edit Manifest`，可对未完成 Idea Run 写入 `work_plan` / `manifest_draft` message、采集 file change evidence、work evidence、verification tool result、截图/测试 artifact 并生成 final archive；审批型 file change 先记录 approval evidence 并把 run 保持在 `deferred`，批准后带 `approvalId` 复用同一 manifest 续跑。Activity 支持按 `agentRunId` 过滤并能反向打开具体 Run，不自动执行危险命令或非 allowlist 命令。
- Codebase Index 二期后端：`CodebaseIndexStore`、`CodebaseQueryEngine`、`/api/codebase-index/*`，返回 path/line/reason/score/anchor/symbols/routes/semanticScore/citation，避免黑盒总结；最新未提交增量增加按 cwd 隔离的 per-file evidence cache，rebuild 时基于 mtime/size/hash 复用未变化文件并在 status 暴露 `cacheStats`；新增 `CodebaseFtsIndex`，将 focus evidence 写入内存 SQLite FTS5 表，query 合并 `fts5/bm25/sqlite-fts` reason、`bm25Rank` 和 `ftsSummary`，并限制 query cache 防止内存 FTS 句柄无限增长；新增 `CodebaseVectorIndex`，基于本地 hash embedding + cosine 对 evidence 文件生成 `local-hash-vector/semantic-vector/vector-index` 信号，status 暴露 `vectorSummary`；`JavaScriptAstAnalyzer` 使用 `@babel/parser` 覆盖 TS/TSX/JSX，并记录 `type-import` / `type-reference` / `type-extends` / `type-implements` / `type-constraint` / `type-assertion` / `type-satisfies` / `type-instantiation` / `member-call` / `member-reference`；`SymbolGraph` 支持 named/default/re-export/renamed import 绑定并能从 barrel exports 连回真实实现，同时保留 `type-*` 与 `member-*` reference kind；新增 `CodebasePersistentIndex` 将 rebuild 后的 status/map 写入本地 SQLite `codebase_index_snapshots`，默认每个 cwd 保留 48 个快照；新增 `CodebaseCitationChain`，为查询结果附加 symbols/anchors/snippets/imports/exports/references 与 Symbol Graph 关联证据；新增 `CodebaseQuestionAnswer` 和 `/api/codebase-index/question`，把 citation 结果汇总为本地 code answer，返回 answer、answerLines、citations、coverage、limitations。
- Crow5 本地研究证据地图：`docs/crow5-local-research-evidence-map.md` 已补齐来源/版本/许可边界、bundle 与用户数据结构、配置/Agent/Skill/SQLite/日志/UI 布局、能力清单、合法可借鉴模式、Xike Lab 自研替代方案、P0-P9 路线和后续无需授权可确认事项。
- Agent Center / Codebase Center UI：Agent 图谱四个 tab、Runs timeline/export、代码索引查询与结果添加到 Dispatch Preview。
- 权限治理扩展：统一 `evaluatePermission()`，覆盖危险 shell、文件写删、外部目录、敏感 `.env`、plugin/MCP、provider/model config、network upload、auto-accept scope。
- 审批后安全重试：HTTP/API 高风险入口可带 `approvalId` 重试；只有原审批 payload 的 action/target 完全匹配且 approved 才放行；pending 复用原审批；rejected/cancelled/mismatch 直接 deny。
- 左侧栏 UI：功能入口已按「工作台 / 治理 / 系统」分组，图标放大并添加名称标签；e2e 已通过。

## 最近验证

当前 P0-A Codebase Index dynamic import / route-to-test / unresolved refs 增量已验证：

```bash
node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseCitationChain.js && node --check src/agents/CodebaseQuestionAnswer.js && node --check public/app.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/code-context-evidence.test.js && node --check tests/unit/symbol-graph.test.js && node --check tests/unit/codebase-index-store.test.js
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0a-codebase-home PORT=52094 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0a-codebase-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52094 npm run test:e2e
lsof -nP -iTCP:52094 -sTCP:LISTEN
```

结果：

- 定向测试：5 个测试文件通过，25 个测试通过。
- 全量测试：62 个测试文件通过，323 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，新增覆盖 Codebase Center parser coverage 与 Local Code Answer limitations 展示；服务已停止，52094 无监听进程。

当前 P0-B Agent Run 归档 artifact 反查增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0b-artifact-home-fresh PORT=52096 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0b-artifact-home-fresh PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52096 npm run test:e2e
lsof -nP -iTCP:52096 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，11 个测试通过，覆盖 artifact 聚合、安全读取、缺失文件、非允许目录和路由下载。
- 全量测试：62 个测试文件通过，324 个测试通过。
- lint / diff check：通过。
- e2e：115/115 通过，覆盖 Gate Audit Report artifact lookup/open、Activity archive artifact reverse lookup、Session Evidence artifact lookup/open；服务已停止，52096 无监听进程。

当前 P0-C Gate 审计对账分区增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p0c-gate-audit-home PORT=52097 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p0c-gate-audit-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52097 npm run test:e2e
lsof -nP -iTCP:52097 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，12 个测试通过，覆盖 gate audit partition mismatch。
- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：115/115 通过，覆盖 Gate Audit Report 中 `Partition Mismatches` 展示；服务已停止，52097 无监听进程。

当前 P1 产品主路径第一刀增量已验证：

```bash
node --check public/app.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-main-path-home-clean PORT=52098 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-main-path-home-clean PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52098 npm run test:e2e
lsof -nP -iTCP:52098 -sTCP:LISTEN
```

结果：

- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：117/117 通过，覆盖 `Idea-to-Archive guided dispatch path`、`Idea-to-Archive guided run path`、final archive、approval resume gate audit、artifact lookup/open 等链路；服务已停止，52098 无监听进程。
- 闭环修复：首次复测发现 Dispatch Preview 预演后主路径条未刷新，仍显示旧的下一步；已新增局部刷新并在输入变化时清空旧 classification，干净 HOME 下复测通过。

当前 P1 产品主路径第二刀增量已验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-main-next-home PORT=52099 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-main-next-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52099 npm run test:e2e
lsof -nP -iTCP:52099 -sTCP:LISTEN
```

结果：

- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：121/121 通过，新增覆盖 `Idea-to-Archive guided next manifest action`、`guided next verify action`、`guided patch alternative`、`guided preflight action`；服务已停止，52099 无监听进程。
- 自检结论：本阶段只收敛 UI 主路径和事件入口，没有改变后端执行/审批/gate/artifact 安全规则；外部账号、密钥、付费授权不涉及。

当前 P1 产品主路径第三刀增量已验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-dedupe-home PORT=52100 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-dedupe-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52100 npm run test:e2e
lsof -nP -iTCP:52100 -sTCP:LISTEN
```

结果：

- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：123/123 通过，新增覆盖 `Idea-to-Archive action bar deduplicated` 与 `Idea-to-Archive guided final archive action`；服务已停止，52100 无监听进程。
- 自检结论：没有删除任何 Idea-to-Archive 能力，只把入口从顶部重复 action bar 收敛到主路径；非 Idea Run 的通用归档入口仍可用。外部账号、密钥、付费授权不涉及。

当前 P1 产品主路径第四刀增量已验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-p1-final-archive-home PORT=52101 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-p1-final-archive-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52101 npm run test:e2e
lsof -nP -iTCP:52101 -sTCP:LISTEN
```

结果：

- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：124/124 通过，新增覆盖 `Idea-to-Archive archive summary focus`；服务已停止，52101 无监听进程。
- 自检结论：本阶段只增强 final archive 的 UI 汇总与滚动聚焦，不改变 archive 数据结构、artifact 安全下载或审批 gate 规则；外部账号、密钥、付费授权不涉及。

当前 P1 Model / Skill Center 收口增量已验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-e2e-52103 PORT=52103 npm start
HOME=/tmp/xikelab-e2e-52103 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52103 npm run test:e2e
lsof -nP -iTCP:52103 -sTCP:LISTEN
```

结果：

- 全量测试：62 个测试文件通过，325 个测试通过。
- lint / diff check：通过。
- e2e：125/125 通过，覆盖 `Model/Skill Center local status`，其中包括 Provider Model Status、No live ping、Model Recommendations、Skill Injection Matrix、Skill Source & Risk 和无明文 API key 展示；服务已停止，52103 无监听进程。
- 自检结论：本阶段只增强 Agent Center 的只读本地状态 tab，没有写 provider 配置、没有展示密钥、没有接入外部账号或 SaaS 租户；健康状态是本地注册与模型提示清单，不做外部 live ping，避免触发账号/额度/网络副作用。

当前 staged diff review 增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-review-... PORT=52085 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52085/?t=... npm run test:e2e
lsof -nP -iTCP:52085 -sTCP:LISTEN
```

结果：

- 定向测试：4 个测试文件通过，13 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：102/102 通过，覆盖 Governance Center 多文件 `Staged Diff +2/-0`、双文件 approval resume 写入/验证、Run detail staged diff、Activity gate audit、Gate Audit Report markdown 与 report archive artifact；服务已停止，52085 无监听进程。

当前 staged diff coverage/risk 增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-coverage-... PORT=52086 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52086/?t=... npm run test:e2e
lsof -nP -iTCP:52086 -sTCP:LISTEN
```

结果：

- 定向测试：4 个测试文件通过，13 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：102/102 通过，覆盖 Governance Center `2/2 verified`、`0 uncovered`、每文件 `coverage verified`/risk、Run detail staged diff coverage、Gate Audit Report `Coverage: 2/2 verified, 0 uncovered` 和 `coverage:verified`；服务已停止，52086 无监听进程。

当前 staged diff 可操作 UI 增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-staged-diff-ui-... PORT=52087 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52087/?t=... npm run test:e2e
lsof -nP -iTCP:52087 -sTCP:LISTEN
```

结果：

- 定向测试：4 个测试文件通过，13 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：103/103 通过，新增覆盖 Governance Center 文件 `<details>` 折叠/展开、coverage command chip 点击高亮全局命令、Risk reasons 展示；服务已停止，52087 无监听进程。

当前 Agent Run session evidence chain 增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-evidence-home PORT=52088 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52088/?t=... npm run test:e2e
lsof -nP -iTCP:52088 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，10 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：104/104 通过，新增覆盖 Agent Run Session Evidence Chain；服务已停止，52088 无监听进程。

当前 command coverage 解释扩展增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/unit/agent-run-approval-resume-review.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-coverage-explain-home-3 PORT=52091 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52091/?t=... npm run test:e2e
lsof -nP -iTCP:52089 -sTCP:LISTEN
lsof -nP -iTCP:52090 -sTCP:LISTEN
lsof -nP -iTCP:52091 -sTCP:LISTEN
```

结果：

- 定向测试：4 个测试文件通过，13 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：104/104 通过，覆盖 `Coverage explanation` UI 展开、safe verification 解释、Gate Audit Report `Coverage Explanations`；服务已停止，52089/52090/52091 无监听进程。
- 闭环修复：首次 e2e 因深层 coverage explanation 进入 gate audit digest 后被 Activity sanitize 裁剪而导致 `Verified: yes` 断言失败；已将解释保留在 UI/报告/staged diff gate 中，但从跨来源 digest 的深层数组比较中移除，复测通过。

当前 Session Evidence markdown UI 导出入口增量已验证：

```bash
node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs && npm test -- tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-store.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-export-home PORT=52091 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52091/?t=... npm run test:e2e
lsof -nP -iTCP:52091 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，10 个测试通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- lint / diff check：通过。
- e2e：106/106 通过，新增覆盖 Session Timeline `Export Session` 按钮和 markdown modal 内容，包括 `# Agent Run Session ...`、`Session Evidence Chain`、activity/run sibling 证据；服务已停止，52091 无监听进程。

当前 Preflight Review coverage filter UI 增量已验证：

```bash
node --check public/app.js && node --check tests/e2e/panel-ui-walkthrough.mjs && npm run lint && npm test -- tests/unit/agent-run-approval-resume-review.test.js tests/unit/routes/governance-routes.test.js
npm test
git diff --check
HOME=/tmp/xikelab-coverage-filter-home PORT=52092 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52092/?t=... npm run test:e2e
lsof -nP -iTCP:52092 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，3 个测试通过；lint 通过。
- 全量测试：62 个测试文件通过，320 个测试通过。
- diff check：通过。
- e2e：107/107 通过，新增覆盖 Governance Center coverage filter、Agent Run session export action 和 session evidence export；服务已停止，52092 无监听进程。

当前 Session Evidence markdown 文件归档入口增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-session-archive-home PORT=52093 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52093/?t=... npm run test:e2e
lsof -nP -iTCP:52093 -sTCP:LISTEN
```

结果：

- 定向测试：2 个测试文件通过，10 个测试通过；语法检查通过。
- 全量测试：62 个测试文件通过，321 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，新增覆盖 Session Timeline `Archive Session` 按钮和 `Session evidence archived:` artifact 展示；服务已停止，52093 无监听进程。

当前 Codebase Index 类型契约证据增量已验证：

```bash
node --check src/agents/CodeContextEvidence.js && node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodebaseCitationChain.js && node --check src/agents/CodebaseQuestionAnswer.js && node --check src/agents/CodebasePersistentIndex.js && node --check src/agents/AgentSkillRegistry.js && node --check public/app.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/symbol-graph.test.js && node --check tests/unit/codebase-index-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/agent-skill-registry.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-type-implementation-home PORT=52092 PANEL_NO_OPEN=1 npm start
PANEL_URL=http://127.0.0.1:52092/?t=... npm run test:e2e
lsof -nP -iTCP:52092 -sTCP:LISTEN
```

结果：

- 定向测试：6 个测试文件通过，40 个测试通过。
- 全量测试：62 个测试文件通过，321 个测试通过。
- lint / diff check：通过。
- e2e：110/110 通过，新增覆盖 Agent preview Symbol Graph `type impl` 计数；服务已停止，52092 无监听进程。
- 闭环修复：首次定向测试发现 `CodeContextEvidence` sanitize 裁掉 method owner 字段，导致 `SymbolGraph` 无法生成 type implementation 引用；已保留 `owner` / `ownerType` 并复测通过。

最近两次功能提交均已跑完整验证：

```bash
npm test
npm run lint
git diff --check
npm run test:e2e
```

最新提交基线记录：

- 单元测试：60 个测试文件通过，291 个测试通过。
- lint：通过。
- diff check：通过。
- e2e：63/63 通过。

当前未提交增量已验证：

```bash
node --check src/agents/JavaScriptAstAnalyzer.js && node --check src/agents/SymbolGraph.js && node --check src/agents/CodeContextEvidence.js && node --check src/agents/CodebaseIndexStore.js && node --check src/agents/CodebaseQueryEngine.js && node --check tests/unit/javascript-ast-analyzer.test.js && node --check tests/unit/symbol-graph.test.js
npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-map.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm test
npm run lint
git diff --check
npm run test:e2e
```

结果：

- Codebase Index / Code Context / Symbol Graph 定向测试：6 个测试文件通过，22 个测试通过。
- 全量单元测试：61 个测试文件通过，309 个测试通过。
- lint / diff check：通过。
- e2e：启动 `npm start` 后 72/72 通过，服务已停止；Codebase Center UI 状态验证包含 vector 索引计数，Agent Run UI 验证包含 Archive Run 按钮、Execution Archive 视图、Idea-to-Archive Run Draft、Auto Verify & Archive 和 final archive。
- 新增覆盖 SQLite FTS5 / BM25 查询合并，断言返回 `fts5`、`bm25`、`sqlite-fts` reason 与 `bm25Rank`；覆盖本地 hash-vector/cosine 查询融合，断言返回 `local-hash-vector`、`semantic-vector`、`vector-index` reason 与 `semanticScore`；覆盖 query cache 上限，避免内存 FTS 句柄无界增长；新增覆盖 TSX AST、type import 跳过运行时引用、default export、barrel re-export、renamed import 绑定，以及 `type-import` / `type-reference` / `type-extends` / `type-implements` / `type-constraint` / `type-assertion` / `type-satisfies` / `type-instantiation` / `member-call` / `member-reference` 进入 Symbol Graph；新增覆盖 SQLite snapshot 冷启动查询、citation chain、每 cwd 快照保留上限。

当前 Agent Run 会话化补强未提交增量已验证：

```bash
node --check src/audit/ActivityLog.js && node --check src/server/routes/activity.js && node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/activity-log.test.js && node --check tests/unit/routes/activity-routes.test.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js
npm test
npm run lint
git diff --check
npm run test:e2e
```

结果：

- Agent Run / Activity 定向测试：4 个测试文件通过，14 个测试通过。
- Agent Run / 治理链路相关测试：10 个测试文件通过，29 个测试通过。
- 全量单元测试：61 个测试文件通过，309 个测试通过。
- lint / diff check：通过。
- e2e：72/72 通过，服务已停止。
- 新增覆盖 governance lineage、budget blocker、autopilot job 关联、Markdown Governance Lineage / Execution Archives 导出、`deferReason` / `hasGovernance` / `budgetIncidentId` route 查询过滤、安全 replay plan 记录、replay result 归档、执行后 archive artifact、Idea-to-Archive Run Draft、intake archive、Complete Idea Run、verification tool result 与 final archive、Activity `agentRunId` 过滤与反向打开 Run，以及历史 budgetIncidentId 不应在非 open / 非 deferred 状态下误判为 blocker。

当前 Idea-to-Archive 自动验证执行器未提交增量已验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-idea-auto-home PORT=52043 npm start
HOME=/tmp/xikelab-idea-auto-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52043 npm run test:e2e
```

结果：

- Agent Run / Activity / 自动验证定向测试：5 个测试文件通过，17 个测试通过。
- Agent Run / 治理链路相关测试：12 个测试文件通过，40 个测试通过。
- 全量单元测试：61 个测试文件通过，309 个测试通过。
- lint：通过。
- e2e：72/72 通过，服务已停止；Idea-to-Archive UI 验证覆盖 `Auto Verify & Archive`、自动验证完成、`Auto verification passed`、verification tool result 与 final archive。
- e2e 清理：删除 2 个测试 run、8 条 message、1 条 tool result、18 条 event。
- 新增覆盖 `AgentRunVerificationExecutor` 命令解析、allowlist 校验、`PermissionGovernance` 判定、`spawn` 非 shell 执行、安全 `node --check` 通过路径、非 allowlist 危险命令执行前阻断、blocked/approval-required evidence、`POST /api/agent-runs/:id/idea-auto-execute` 与 Runs tab 自动归档入口。

当前 Idea-to-Archive work evidence + 自动验证归档未提交增量已验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/agents/AgentRunStore.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/permission-governance.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-work-evidence-home PORT=52044 npm start
HOME=/tmp/xikelab-work-evidence-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52044 npm run test:e2e
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh open 'http://127.0.0.1:52044/?t=...'
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh snapshot
CODEX_HOME=/Users/hxx/.codex /Users/hxx/.codex/skills/playwright/scripts/playwright_cli.sh screenshot --filename output/playwright/xikelab-idea-work-evidence.png --full-page
```

结果：

- 新增 executor 单测：1 个测试文件通过，4 个测试通过。
- Agent Run / Activity / 权限治理定向测试：6 个测试文件通过，26 个测试通过。
- Agent Run / 治理链路相关测试：12 个测试文件通过，41 个测试通过。
- 全量单元测试：61 个测试文件通过，310 个测试通过。
- lint：通过。
- e2e：73/73 通过，服务已停止；新增覆盖 `Auto Work + Verify`、`Idea work plan prepared`、`git status --short`、`git diff --name-only`、`git diff --check`、verification tool result 与 final archive。
- Playwright CLI 可视复查通过：Runs tab 的 succeeded `idea_to_archive` detail 中显示 `work_plan` message、work evidence tool results、verification results 和 Execution Archive；截图：`output/playwright/xikelab-idea-work-evidence.png`。
- 新增覆盖 `validateWorkEvidenceCommand()`、只读 git work evidence allowlist、`work_plan` message、work evidence tool result、final archive external evidence 中的 `workPlan` / `workEvidence`，以及非 allowlist 命令执行前阻断。

当前 Idea-to-Archive 受治理文件改动 + 截图证据挂载未提交增量已验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/permission-governance.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
git diff --check
HOME=/tmp/xikelab-file-change-home-2 PORT=52048 npm start
HOME=/tmp/xikelab-file-change-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52048 npm run test:e2e
```

结果：

- 新增 executor 单测：1 个测试文件通过，5 个测试通过。
- Agent Run / Activity / 权限治理定向测试：6 个测试文件通过，27 个测试通过。
- Agent Run / 治理链路相关测试：12 个测试文件通过，42 个测试通过。
- 全量单元测试：61 个测试文件通过，311 个测试通过。
- lint / diff check：通过。
- e2e：75/75 通过，服务已停止；新增覆盖受治理 `file.write output/playwright/e2e-idea-work-generated.js`、`file changes 1`、`artifacts 1`、截图 evidence exists、`node --check output/playwright/e2e-idea-work-generated.js` 与 final archive。
- 新增覆盖 `validateFileChange()`、safe project file allowlist、外部路径/`.env` 阻断、`file.write` permission decision、before/after sha256、artifact metadata、文件改动 tool result 与 archive external evidence。

当前 Idea-to-Archive UI manifest editor 未提交增量已验证：

```bash
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-verification-executor.test.js
npm run lint
git diff --check
npm test
HOME=/tmp/xikelab-ui-manifest-home PORT=52049 npm start
HOME=/tmp/xikelab-ui-manifest-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52049 npm run test:e2e
```

结果：

- executor 单测：1 个测试文件通过，5 个测试通过。
- 全量单元测试：61 个测试文件通过，311 个测试通过。
- lint / diff check：通过。
- e2e：76/76 通过，服务已停止；新增覆盖 `Edit Manifest` UI、JSON manifest 提交、受治理 `file.write`、自定义 `node --check`、`file changes 1` 与 `artifacts 1`。
- in-app Browser 复查通过：queued `idea_to_archive` detail 同时显示 `Auto Work + Verify` 与 `Edit Manifest`；截图：`output/playwright/xikelab-ui-manifest-editor-browser.png`。

当前 Idea-to-Archive approval resume file change 未提交增量已验证：

```bash
node --check src/permissions/PermissionGovernance.js && node --check src/agents/AgentRunVerificationExecutor.js && node --check public/app.js && node --check tests/unit/permission-governance.test.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs
git diff --check
npm test -- tests/unit/permission-governance.test.js tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-approval-resume-home-2 PORT=52052 npm start
HOME=/tmp/xikelab-approval-resume-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52052 npm run test:e2e
```

结果：

- 权限治理 + executor 定向测试：2 个测试文件通过，15 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，44 个测试通过。
- 全量单元测试：61 个测试文件通过，313 个测试通过。
- lint / diff check：通过。
- e2e：77/77 通过，服务已停止；新增覆盖 `requiresApproval` manifest 首次执行转 `deferred`、审批通过、带同一 `approvalId` 续跑、`file.write` 成功、`file changes 1` 进入 final archive。
- in-app Browser 复查尝试时 Browser 插件 `iab` 当前不可用；该项未作为阻塞，因同一浏览器交互链路已由 Playwright e2e 实机通过。

当前 Idea-to-Archive expanded command policy 未提交增量已验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-command-policy-home PORT=52054 npm start
HOME=/tmp/xikelab-command-policy-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52054 npm run test:e2e
```

结果：

- executor 单测：1 个测试文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，45 个测试通过。
- 全量单元测试：61 个测试文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：78/78 通过，服务已停止；新增覆盖 `node --test output/playwright/e2e-node-policy-*.mjs`、`git status --porcelain=v1`、`git diff --stat`、受治理 `file.write`、`file changes 1` 与 final archive。
- 新增覆盖 exact allowlisted `npm run`（禁止额外参数）、project-local `node --test`、selected `node scripts/*`、只读 git work evidence 扩展，以及外部路径/未知脚本/非 allowlist git 命令执行前阻断。

当前 Idea-to-Archive generated work manifest fileChanges 未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-work-manifest-home PORT=52056 npm start
HOME=/tmp/xikelab-work-manifest-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52056 npm run test:e2e
```

结果：

- Agent Run store/route 定向测试：2 个测试文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，45 个测试通过。
- 全量单元测试：61 个测试文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：80/80 通过，服务已停止且 52056 无 LISTEN 进程；新增覆盖 `Generate Manifest`、`manifest_draft` message、`agent.run.idea_manifest_drafted` Activity、`Edit Manifest` 预填带 `fileChanges` 的 generated work manifest、受治理 `file.write output/playwright/idea-work-agent-run-e2e-generated-manifest-*.md`、`file changes 1`、继续执行 `node --check public/app.js`、只读 work evidence 与 final archive。
- 新增能力不会在生成 draft 时自动写盘或执行命令；draft 只记录可编辑 manifest 和工作清单 artifact，真正执行仍走 `idea-auto-execute` 的 allowlist、`PermissionGovernance`、work evidence、verification 和 archive 链路。

当前 Idea-to-Archive 本地 Agent file-change plan 未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-agent-manifest-home-3 PORT=52059 npm start
HOME=/tmp/xikelab-agent-manifest-home-3 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52059 npm run test:e2e
```

结果：

- Agent Run store/route 定向测试：2 个测试文件通过，7 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，45 个测试通过。
- 全量单元测试：61 个测试文件通过，314 个测试通过。
- lint / diff check：通过。
- e2e：80/80 通过，服务已停止且 52059 无 LISTEN 进程；新增覆盖 generated manifest 预填包含 `output/playwright/idea-agent-change-*.js`、`local-agent-filechange-synthesizer`、Agent JS 计划的 `node --check`，执行后 final archive 显示 `file changes 2`，并记录两个受治理 `file.write`。
- 账号/密钥/外部授权：本阶段未接入外部付费模型；采用本地 deterministic Agent synthesizer，避免新增账号、API Key、登录、额度或付款风险。后续如发现项目已有明确可用模型凭证，可把同一 manifest schema 接入模型 adapter。

当前 Idea-to-Archive 可选模型 adapter source patch manifest 未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-model-patch-home-2 PORT=52061 npm start
HOME=/tmp/xikelab-model-patch-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52061 npm run test:e2e
```

结果：

- Agent Run store/route 定向测试：2 个测试文件通过，8 个测试通过。
- Agent Run / Activity / 权限治理相关测试：12 个测试文件通过，46 个测试通过。
- 全量单元测试：61 个测试文件通过，315 个测试通过。
- lint / diff check：通过。
- e2e：81/81 通过，服务已停止且 52061 无 LISTEN 进程；新增覆盖 `Generate Patch`、`idea_patch_manifest_draft`、默认 `useModel:false` 本地 fallback、`Edit Manifest` 预填 append 到 `public/app.js` 的 source patch proposal、`node --check public/app.js`、`git status --porcelain=v1` 与 `git diff --stat`。
- 账号/密钥/外部授权：本阶段 UI 默认不调用外部模型、不新增账号/API Key/登录/额度风险；当前环境只看到 `CLAUDE_BIN_PATH` 与 Anthropic base URL 备份/主变量，未看到 `OPENAI_API_KEY`。API 路径可显式 `useModel:true` 使用已有 adapter；adapter 不可用、调用失败或返回 JSON 不安全时记录 generation error 并降级到本地 fallback。

当前 P0 Governance Center 最小统一入口 + 预算处理动作未提交增量已验证：

```bash
node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/routes/governance-routes.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/routes/governance-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/agent-run-lifecycle.test.js tests/unit/room-adapter-budget.test.js tests/unit/routes/delegations-routes.test.js tests/unit/delegation-autostart.test.js tests/unit/budget-policy-store.test.js tests/unit/metrics-store-agent-diagnostics.test.js tests/unit/permission-governance.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-center-actions-home-2 PORT=52065 npm start
HOME=/tmp/xikelab-governance-center-actions-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52065 npm run test:e2e
```

结果：

- Governance route 定向测试：1 个测试文件通过，2 个测试通过。
- Governance / Activity / Agent Run 定向测试：5 个测试文件通过，17 个测试通过。
- Agent Run / Activity / 权限治理相关测试：13 个测试文件通过，48 个测试通过。
- 全量单元测试：61 个测试文件通过，315 个测试通过。
- lint / diff check：通过。
- e2e：87/87 通过，服务已停止且 52065 无 LISTEN 进程；新增覆盖侧边栏 `btnGovernance`、Governance Center modal、KPI、Next Actions、Open Items、Agent Runs、Recent Activity、刷新按钮，以及真实创建 warning budget incident 后在 Governance Center 直接 `标记已处理` 并刷新消失。
- 可视复查：Governance Center 截图已保存到 `output/playwright/xikelab-governance-center.png`，52063 临时服务已停止且无 LISTEN 进程。

当前 P0 Agent Run session-level 聚合未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check src/server/routes/agentRuns.js && node --check public/app.js && node --check tests/unit/agent-run-store.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js tests/unit/agent-run-verification-executor.test.js
npm test
npm run lint
HOME=/tmp/xikelab-agent-run-session-home-2 PORT=52067 npm start
HOME=/tmp/xikelab-agent-run-session-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52067 npm run test:e2e
```

结果：

- Agent Run store / route 定向测试：2 个测试文件通过，9 个测试通过。
- Agent Run / Governance / Activity 相关测试：6 个测试文件通过，25 个测试通过。
- 全量单元测试：61 个测试文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：88/88 通过，服务已停止且 52066 / 52067 无 LISTEN 进程；新增覆盖 Runs tab `sessionId` 筛选、`Session Timeline`、同一 session 的 sibling run 聚合、Activity 反向打开 run 后展示 session-level counts。

当前 P0 Governance Center 审批续跑入口未提交增量已验证：

```bash
node --check src/agents/AgentRunVerificationExecutor.js && node --check src/server/routes/agentRuns.js && node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/agent-run-verification-executor.test.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-approval-resume-home-3 PORT=52070 npm start
HOME=/tmp/xikelab-governance-approval-resume-home-3 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52070 npm run test:e2e
```

结果：

- Agent Run / Governance / Permission / Activity 定向测试：7 个测试文件通过，34 个测试通过。
- 全量单元测试：61 个测试文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：90/90 通过，服务已停止且 52070 无 LISTEN 进程；新增覆盖 Governance Center `Approval Actions`、deferred `idea_to_archive` 审批项展示、`批准并续跑`、同一 approvalId + 同一 manifest 的安全恢复写入、`file.write`、`node --check` 与 Execution Archive。
- 安全边界：该入口只对已关联 pending approval 的 deferred `idea_to_archive` run 生效；先走原审批批准，再调用 `approval-resume` 复用 run 中保存的 manifest，最终仍由 `PermissionGovernance` 校验 action/target/content hash，不自动重放危险终端命令。

当前 P0 Governance Center 执行前 diff 审查未提交增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/server/routes/agentRuns.js && node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-governance-preflight-review-home PORT=52071 npm start
HOME=/tmp/xikelab-governance-preflight-review-home PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52071 npm run test:e2e
```

结果：

- Agent Run / Governance / Permission / Activity 定向测试：7 个测试文件通过，34 个测试通过。
- 全量单元测试：61 个测试文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：91/91 通过，服务已停止且 52071 无 LISTEN 进程；新增覆盖 Governance Center `Preflight Review`、待写文件路径、新增内容 diff preview、验证命令、随后 `批准并续跑` 和 Execution Archive。
- 新增能力：`AgentRunApprovalResumeReview` 从 deferred run 保存的 resume manifest 生成只读审查摘要；`GET /api/agent-runs/:id/approval-resume-preview` 可单独返回 review；`/api/governance/summary` 会把 review 挂到可续跑 approval 上。该 review 不写盘、不执行命令，真正执行仍走原 `approval-resume` + `PermissionGovernance` 链路。

当前 P0 模型 adapter source patch 质量评估未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js && node --check tests/unit/agent-run-store.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm test -- tests/unit/agent-run-verification-executor.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/permission-governance.test.js tests/unit/agent-run-store.test.js tests/unit/routes/activity-routes.test.js tests/unit/activity-log.test.js
npm test
npm run lint
HOME=/tmp/xikelab-patch-quality-home-2 PORT=52073 npm start
HOME=/tmp/xikelab-patch-quality-home-2 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52073 npm run test:e2e
```

结果：

- Agent Run store/route 定向测试：2 个测试文件通过，9 个测试通过。
- Agent Run / Governance / Permission / Activity 定向测试：7 个测试文件通过，34 个测试通过。
- 全量单元测试：61 个测试文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：92/92 通过，服务已停止且 52073 无 LISTEN 进程；新增覆盖 `Generate Patch` 后 Run detail 中出现 `Patch quality <grade> <score>/100` 与 `proposal_only_patch` finding，再继续验证 source patch manifest 预填。
- 新增能力：`sourcePatchQuality()` 会对 source patch manifest 的文件改动数、是否命中 affected files、`git diff --check`、syntax/test 命令、work evidence、fallback/model 来源、proposal-only patch 和大内容风险进行本地评分；分数和 findings 写入 `manifestDraft.patchQuality`、message summary 和 `agent.run.idea_patch_manifest_drafted` Activity。

当前 P0 approve-before-run diff gate 未提交增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js && node --check src/server/routes/agentRuns.js && node --check src/server/routes/governance.js && node --check public/app.js && node --check tests/unit/routes/agent-runs-routes.test.js && node --check tests/unit/routes/governance-routes.test.js && node --check tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/agent-run-store.test.js tests/unit/agent-run-verification-executor.test.js
npm test
npm run lint
HOME=/tmp/xikelab-e2e-gate-1779744510 PORT=52075 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-e2e-gate-1779744510 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52075 npm run test:e2e
```

结果：

- Agent Run / Governance route 定向测试：4 个测试文件通过，18 个测试通过。
- 全量单元测试：61 个测试文件通过，316 个测试通过。
- lint / diff check：通过。
- e2e：92/92 通过，服务已停止且 52075 无 LISTEN 进程；新增覆盖 Approval Actions 按钮携带 `review-*` gate id 与 64 位 sha、Preflight Review 显示 `Gate review-*`，点击前重新拉取 preview 后批准并续跑，最终写入文件并生成 Execution Archive。
- 新增能力：`AgentRunApprovalResumeReview` 现在会基于 runId、approvalId、文件 before/content sha、命令安全状态和风险列表生成 deterministic review gate；`GET /api/agent-runs/:id/approval-resume-preview` 返回 `resumeReviewGate`；`POST /api/agent-runs/:id/approval-resume` 必须收到匹配的 gate id/hash，缺失返回 428，过期或不匹配返回 409 且不会调用 executor；成功续跑响应包含 `resumeReviewGate`。前端会在批准原审批前重新拉取 preview 比对 gate，避免先批准再发现 diff 漂移。

当前 P0 Codebase Index code question UI 未提交增量已验证：

```bash
node --check src/agents/CodebaseQuestionAnswer.js src/agents/CodebaseIndexStore.js src/server/routes/codebaseIndex.js public/app.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js tests/e2e/panel-ui-walkthrough.mjs && git diff --check
npm test -- tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js
npm test
npm run lint
HOME=/tmp/xikelab-code-question-1779744824 PORT=52076 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-code-question-1779744824 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52076 npm run test:e2e
```

结果：

- Codebase Index store/route 定向测试：2 个测试文件通过，11 个测试通过。
- 全量单元测试：61 个测试文件通过，317 个测试通过。
- lint / diff check：通过。
- e2e：93/93 通过，服务已停止且 52076 无 LISTEN 进程；新增覆盖 Codebase Center `Answer`、`Local Code Answer`、`C1` citation、`public/app.js` 命中，以及原有“添加结果到 Dispatch Preview”链路。
- 新增能力：`POST /api/codebase-index/question` 复用现有 query/FTS/vector/citation 结果，返回 deterministic `answer`、`answerLines`、`citations`、coverage、limitations；UI 只做本地证据汇总，不调用模型、不上传代码、不替代 path/line/reason/citation。

当前 Codebase Answer -> Dispatch / Agent Run 证据链未提交增量已验证：

```bash
node --check src/agents/AgentSkillRegistry.js
node --check src/server/routes/agentRegistry.js
node --check src/server/routes/agentRuns.js
node --check src/agents/AgentRunStore.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-registry-routes.test.js tests/unit/routes/agent-runs-routes.test.js
git diff --check
npm test
npm run lint
HOME=/tmp/xikelab-code-answer-dispatch-1779745702b PORT=52078 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-code-answer-dispatch-1779745702b PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52078 npm run test:e2e
```

结果：

- Agent Run / Agent Registry 定向测试：3 个测试文件通过，18 个测试通过。
- 全量单元测试：61 个测试文件通过，317 个测试通过。
- lint / diff check：通过。
- e2e：96/96 通过，服务已停止且 52078 无 LISTEN 进程；新增覆盖 Codebase Answer 添加到 Dispatch Preview、Dispatch prompt preview 展示 `Code Question Answer` / `C1`、从 Dispatch 创建 Idea Run 后 Run detail 中可见同一 code answer 证据。
- 新增能力：`codebaseQuestionAnswer` 现在会进入 Dispatch Preview state、`/api/agent-registry/classify` prompt preview、`AgentRunStore.createIdeaRun()` 的 details / decision / summary / intake archive，并在 `buildPatchPrompt()` 中作为 source patch prompt 的本地证据输入；仍只保存 path/line/citation 摘要，不执行命令、不上传代码、不调用模型。

当前 Approval Resume Gate 审计查询未提交增量已验证：

```bash
node --check src/agents/AgentRunApprovalResumeReview.js
node --check src/agents/AgentRunStore.js
node --check src/agents/AgentRunVerificationExecutor.js
node --check src/server/routes/agentRuns.js
node --check src/server/routes/governance.js
node --check public/app.js
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/routes/governance-routes.test.js tests/unit/agent-run-verification-executor.test.js
git diff --check
npm test
npm run lint
HOME=/tmp/xikelab-gate-audit-1779746429 PORT=52079 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-gate-audit-1779746429 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52079 npm run test:e2e
```

结果：

- Agent Run / Governance / Executor 定向测试：4 个测试文件通过，19 个测试通过。
- 全量单元测试：61 个测试文件通过，318 个测试通过。
- lint / diff check：通过。
- e2e：97/97 通过，服务已停止且 52079 无 LISTEN 进程；新增覆盖 approval resume 成功后 Run detail 显示 `Approval Resume Gate`、`review-*`、审批恢复生成文件路径和 `node --check ...` 命令。
- 新增能力：`buildApprovalResumeGateAudit()` 生成可归档 gate audit；`approval-resume-preview` 和 Governance summary 返回 preview audit；`approval-resume` 成功前调用 `recordApprovalResumeGateAudit()` 写入 run details、decision message 和 `agent.run.approval_resume_gate_accepted` Activity；最终 `idea-auto-execute` archive evidence 也携带 `resumeReviewGate` / `resumeReviewGateAudit`。

当前 Agent Runs gate audit 过滤未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
git diff --check
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js
npm run lint
npm test
HOME=/tmp/xikelab-gate-filter-1779747110 PORT=52080 PANEL_NO_OPEN=1 npm start
HOME=/tmp/xikelab-gate-filter-1779747110 PLAYWRIGHT_BROWSERS_PATH=/Users/hxx/Library/Caches/ms-playwright PANEL_URL=http://127.0.0.1:52080 npm run test:e2e
```

结果：

- Agent Run store/route 定向测试：2 个测试文件通过，10 个测试通过。
- 全量单元测试：61 个测试文件通过，318 个测试通过。
- lint / diff check：通过。
- e2e：98/98 通过，服务已停止且 52080 无 LISTEN 进程；新增覆盖在 Run detail 拿到 `review-*` 后，用 Runs tab 的 `reviewGateId` 筛选反查到对应 approval resume run。
- 新增能力：`AgentRunStore.list()` 支持 `approvalResumeGateId` / `reviewGateId` 和 `approvalResumeGateSha256` / `reviewSha256`，从 details JSON 中匹配 `approvalResumeGateId`、`approvalResumeGateAudit.id`、`approvalResumeGateSha256` 或 `approvalResumeGateAudit.sha256`；`parseListQuery()` 和 Runs tab 对应新增 gate id/hash 筛选输入。

当前 Activity gate audit 深链未提交增量已验证：

```bash
node --check src/audit/ActivityLog.js
node --check src/server/routes/activity.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-activity-gate-rerun-1779747710 PORT=52082 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52082/?t=...' npm run test:e2e
```

结果：

- Activity Log / route 定向测试：2 个测试文件通过，8 个测试通过。
- 全量单元测试：61 个测试文件通过，319 个测试通过。
- lint / diff check：通过。
- e2e：100/100 通过，服务已停止且 52082 无 LISTEN 进程；新增覆盖用 `reviewGateId` 打开 Activity Center、列表命中 `agent.run.approval_resume_gate_accepted`、详情显示 `Approval Resume Gate` 面板，并从 Activity gate audit 事件直接打开对应 approval resume Agent Run。
- 新增能力：`ActivityLog.list()` 与 `/api/activity` 支持 `approvalResumeGateId` / `reviewGateId` 和 `approvalResumeGateSha256` / `reviewSha256` 过滤；Activity Center 增加 `reviewGateId` / `reviewSha256` 输入、gate hint、`Approval Resume Gate` 详情面板、gate counts/files/commands 摘要和 Run 反跳按钮。

当前 Gate audit report 对账导出未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-gate-report-1779748534 PORT=52083 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52083/?t=...' npm run test:e2e
```

结果：

- Agent Run / Activity 定向测试：4 个测试文件通过，18 个测试通过。
- 全量单元测试：61 个测试文件通过，319 个测试通过。
- lint / diff check：通过。
- e2e：101/101 通过，服务已停止且 52083 无 LISTEN 进程；新增覆盖从 approval resume Agent Run 打开 `Gate Audit Report`，报告包含 gate id、`Verified: yes`、`agent.run.approval_resume_gate_accepted` Activity source 和 archive source。
- 新增能力：`AgentRunStore.getApprovalResumeGateAuditReport()` 会把 run details、decision message、Activity、archive evidence 中的 approval resume gate audit 归一化、生成 digest、列出 checks/sources/mismatches，并在四类来源一致时标记 `verified=true`；`GET /api/agent-runs/:id/approval-resume-gate-audit?format=json|markdown` 暴露同一报告；`exportRun()` 的 JSON/Markdown 导出也带 gate audit report 摘要；Run detail 的 `Approval Resume Gate` 区块新增 `Gate Audit Report` 按钮。

当前 Gate audit report 文件落盘归档未提交增量已验证：

```bash
node --check src/agents/AgentRunStore.js
node --check src/server/routes/agentRuns.js
node --check public/app.js
node --check tests/e2e/panel-ui-walkthrough.mjs
npm test -- tests/unit/agent-run-store.test.js tests/unit/routes/agent-runs-routes.test.js tests/unit/activity-log.test.js tests/unit/routes/activity-routes.test.js
npm run lint
npm test
git diff --check
HOME=/tmp/xikelab-gate-report-artifact-1779748958 PORT=52084 PANEL_NO_OPEN=1 npm start
PANEL_URL='http://127.0.0.1:52084/?t=...' npm run test:e2e
```

结果：

- Agent Run / Activity 定向测试：4 个测试文件通过，18 个测试通过。
- 全量单元测试：61 个测试文件通过，319 个测试通过。
- lint / diff check：通过。
- e2e：102/102 通过，服务已停止且 52084 无 LISTEN 进程；新增覆盖点击 `Archive Report` 后，Run detail 的 Execution Archive 显示 `Gate audit report archived` 和 `output/playwright/gate-audit-reports/agent-run-e2e-approval-resume-*.md`。
- 新增能力：`AgentRunStore.recordApprovalResumeGateAuditReportArtifact()` 将 gate audit report markdown 写入 `output/playwright/gate-audit-reports/`，记录 artifact path/size/sha256/reportId/gateId/verified，并调用 `recordArchive()` 把 `gateAuditReportArtifact` 和 `resumeReviewGateAudit` 写入 archive evidence；`POST /api/agent-runs/:id/approval-resume-gate-audit/archive` 暴露该能力；Run detail 的 `Approval Resume Gate` 区块新增 `Archive Report` 按钮。

## 后续详细计划

优先继续 P0，不要回到泛泛研究。每一阶段都按「实现 -> 单测/路由测试 -> e2e/替代实机验证 -> 文档交接 -> 自检」收口；如果当前工作区仍然很脏，先用 `git diff --name-only` 分清本阶段触碰文件，禁止为了清理而回退已有 P0 增量。

### P0-A. Codebase Index 三期：Tree-sitter/LSP 级证据深化

目标：在现有 Acorn/Babel AST、SQLite FTS/BM25、本地 hash-vector、persistent snapshot、citation chain、code question 和 `type-implementation` 方法契约证据之上，继续提升代码证据的准确性，尤其是跨文件、跨语言、动态访问和类型关系。

具体任务：

- 引入 Tree-sitter 或等价本地 parser adapter 的抽象层，但先以 JS/TS 为第一语言，不一次性扩到所有语言。
- 给 `CodeContextEvidence` / `JavaScriptAstAnalyzer` / `SymbolGraph` 增加更细的引用类型：dynamic import、object property flow、callback registration、route handler binding、test assertion target。
- 在 `CodebaseCitationChain` 中把类型实现证据、调用证据、route usage、测试 anchor 串成可读 citation path，例如 `API route -> handler -> service method -> test`。
- 让 `CodebaseQuestionAnswer` 能输出“证据不足”的明确限制，不允许凭 query keyword 过度总结。
- 前端 Codebase Center 展示新增 summary：parser coverage、type implementation count、route-to-test chain count、unresolved reference count。

涉及文件：

- `src/agents/JavaScriptAstAnalyzer.js`
- `src/agents/CodeContextEvidence.js`
- `src/agents/SymbolGraph.js`
- `src/agents/CodebaseCitationChain.js`
- `src/agents/CodebaseQuestionAnswer.js`
- `src/agents/CodebaseIndexStore.js`
- `src/agents/CodebaseQueryEngine.js`
- `src/server/routes/codebaseIndex.js`
- `public/app.js`
- `tests/unit/javascript-ast-analyzer.test.js`
- `tests/unit/code-context-evidence.test.js`
- `tests/unit/symbol-graph.test.js`
- `tests/unit/codebase-index-store.test.js`
- `tests/unit/routes/codebase-index-routes.test.js`
- `tests/e2e/panel-ui-walkthrough.mjs`

验收标准：

- `npm test -- tests/unit/javascript-ast-analyzer.test.js tests/unit/code-context-evidence.test.js tests/unit/symbol-graph.test.js tests/unit/codebase-index-store.test.js tests/unit/routes/codebase-index-routes.test.js` 通过。
- `npm run lint`、`npm test`、`git diff --check` 通过。
- e2e 中 Codebase Center 能显示新增证据摘要，并能把相关 code answer 注入 Dispatch Preview / Agent Run。
- 不上传代码、不调用外部模型、不依赖云索引。

进入下一阶段条件：query 结果能稳定返回 path/line/reason/citation，新增证据有单测覆盖，前端显示不破坏现有 Codebase Center 和 Agent Dispatch。

### P0-B. Agent Run 归档 artifact 反查与下载列表（已完成 2026-05-26 09:42 CST）

目标：Session Evidence markdown 文件归档和 Gate Audit Report 文件归档已经能写盘，现在用户能在 UI 里按 run/session/gate 找回这些 artifact，而不是只能在 Execution Archive 里看到最近三条摘要。

具体任务：

- 已在 `AgentRunStore.getTimeline()` 中聚合 archive artifacts，按 `kind/path/sessionId/gateId/reportId/sha256` 建立可查询摘要。
- `GET /api/agent-runs/:id` 返回 `artifacts`，并新增只读 `GET /api/agent-runs/:id/artifacts`。
- Runs tab 新增 `Execution Artifacts` 列表：path、kind、size、sha256 前缀、所属 session/gate，并提供 Copy Path / Open Artifact。
- 新增只读下载 route `GET /api/agent-runs/:id/artifacts/:artifactId/download`。下载只允许 `output/playwright/gate-audit-reports/` 和 `output/playwright/session-evidence/` 下的已记录 artifact。
- Activity Center 可从 `agent.run.archived` 事件查看 `Archive Artifacts` 并打开对应 markdown artifact。

涉及文件：

- `src/agents/AgentRunStore.js`
- `src/server/routes/agentRuns.js`
- `src/audit/ActivityLog.js`（仅在需要 artifact 过滤时）
- `src/server/routes/activity.js`（仅在需要 artifact 过滤时）
- `public/app.js`
- `public/style.css`
- `tests/unit/agent-run-store.test.js`
- `tests/unit/routes/agent-runs-routes.test.js`
- `tests/e2e/panel-ui-walkthrough.mjs`

验收标准：

- 单测已覆盖 artifact 聚合、safe path 限制、缺失文件/越界路径拒绝。
- e2e 已覆盖 Session Evidence artifact 与 Gate Audit Report artifact 同时可见、可反查、可读/下载。
- 52096 端口服务停止后已确认无 LISTEN。

进入下一阶段条件：已满足。P0-C 第一刀也已完成，下一步优先进入 P1 产品主路径或继续 P0-A Tree-sitter/LSP 级证据深化。

### P0-C. Gate 审计对账细化（已完成第一刀 2026-05-26 09:55 CST）

目标：当前 gate audit 已能对 run details、decision message、Activity、archive evidence 做 digest 对账。本轮已把 mismatch 拆成更精确的可行动原因，方便本地治理闭环。

具体任务：

- 已对 gate audit digest 拆分为 file digest、command digest、risk digest、coverage digest、artifact digest。
- Gate Audit Report 中已显示 mismatch 来源：哪个 source、哪个 partition、缺字段还是 digest 不一致。
- Governance Center / Run detail 对 `approval_resume_gate_accepted` 事件提供一键筛选同 gate 所有审计来源。
- 已保持深层 `coverageExplanations` 不参与跨来源 digest，只作为报告明细；分区 digest 也避开 Activity sanitize 会裁剪的展示数组，避免再次引入误报。

涉及文件：

- `src/agents/AgentRunStore.js`
- `src/agents/AgentRunApprovalResumeReview.js`
- `src/audit/ActivityLog.js`
- `src/server/routes/agentRuns.js`
- `public/app.js`
- `tests/unit/agent-run-approval-resume-review.test.js`
- `tests/unit/agent-run-store.test.js`
- `tests/e2e/panel-ui-walkthrough.mjs`

验收标准：

- 构造一致/不一致 gate audit 的单测已给出明确 mismatch partition/reason。
- e2e Gate Audit Report 仍显示 `Verified: yes`，并覆盖 `Partition Mismatches` 展示。
- 未改变 approval resume 的安全门槛：既有路由单测继续覆盖缺 gate 428、stale/mismatch gate 409。

进入下一阶段条件：已满足。审计报告现在能回答“为什么不一致”，而不只是告诉用户不一致。

### P1. 产品主路径：Idea -> Plan -> Work -> Verify -> Archive（第一刀已完成 2026-05-26 10:07 CST）

目标：把已经落地的 Agent Registry、Codebase Answer、Idea Run、Manifest Draft、Auto Work + Verify、Preflight Review、Archive 串成一个用户可理解的主工作流。

具体任务：

- 已在 Agent Center Dispatch Preview 顶部增加 `Idea-to-Archive Path`：一句话输入 -> Code Context -> Dispatch Preview -> Run Draft，显示当前状态和下一步。
- 已在 `idea_to_archive` Run detail 增加主路径步骤：Idea -> Dispatch -> Manifest/Patch -> Work + Verify -> Preflight -> Archive，并根据 run/timeline/artifact/gate 状态显示下一步按钮。
- 已补 `Recommended next` 主按钮：Run Draft 推荐 `Generate Manifest`，manifest draft 后推荐 `Auto Work + Verify`，审批挂起推荐 `Open Preflight Review`，gate 存在时推荐 `Gate Audit Report`，完成后推荐 `Archive Run`；`Generate Patch` 和自定义 manifest 保留为次级动作。
- 已减少顶部传统 action bar 与主路径动作重复：Idea-to-Archive 专属动作集中到主路径，顶部仅保留通用治理/Replay/Activity；非 Idea Run 的 `Archive Run` 保持不变。
- 已把 final archive 收口做成更明确的归档摘要：主路径显示 `Final archive` summary、tools/files/artifacts/blockers，并推荐 `Review Archive` 聚焦到 Execution Archive；`Add Archive Note` 和 artifact 入口作为次级动作。
- 后续可继续深化 P1 Model / Skill Center 的 provider 健康检查、模型推荐解释和 Skill 冲突/注入统计，或回到 P0-A 做 Tree-sitter/LSP 级证据深化。
- 生成 final archive 时展示任务目标、涉及文件、验证命令、截图/测试 artifact、后续行动。
- 保留所有本地治理边界：高风险 file change 仍先 defer，审批后仍需匹配 gate。

涉及文件：

- `public/app.js`
- `public/style.css`
- `src/server/routes/agentRegistry.js`
- `src/server/routes/agentRuns.js`
- `src/agents/AgentRunStore.js`
- `src/agents/AgentRunVerificationExecutor.js`
- e2e walkthrough

验收标准：

- e2e 已覆盖一个任务从 idea 输入、分派、manifest/patch、验证、审计、归档完整走通，并新增 guided dispatch/run path 断言。
- 用户已能在 Dispatch Preview 和 Run detail 看到当前 run 卡在 Idea / Dispatch / Manifest/Patch / Work / Preflight / Archive 的哪一步；后续继续收敛成更少入口。

### P1. Model / Skill Center 本地化（第一刀已完成 2026-05-26 10:37 CST）

目标：学习 Crow5 的 provider/model/skill 管理体验，但保持 Xike Lab 本地优先。

具体任务：

- 已新增 Agent Center `Models/Skills` tab：只读聚合本地 provider 状态、前端模型候选清单、enabled skills、profile skill coverage、missing bindings 和 dispatch hint 缺口。
- 已明确 UI 安全边界：local status only、no secrets shown、provider config read-only；只消费本地只读 API，不写配置、不展示密钥、不接云账号或 SaaS 租户。
- 后续 Provider registry UI：补最近错误、健康检查、可用性原因。
- 后续 Model picker：按 dispatch tag 推荐模型，例如 verification 用低成本快模型，architecture 用强模型，privacy/local 用本地模型，并展示推荐来源。
- 后续 Skill Center：显示 Skill 来源、冲突、重复、prompt 体量和最近注入统计。
- 所有 provider/API key 处理遵守当前安全边界：已有环境变量/本机登录可复用；新付费、绑卡、验证码必须记录为人工授权阻塞点。

验收标准：

- 用户不用编辑 JSON，也能知道当前模型/Skill 是否可用，以及为什么被注入。
- 不把 provider 管理做成云账号或 SaaS 租户系统。

### P2. 权限治理 UI 闭环

目标：后端已有 `approvalId` 安全重试，Governance Center 已补 `idea_to_archive` 审批后续跑；后续补其他高风险 UI 的人工审批后重试，但必须保持 action/target 精确绑定。

具体任务：

- 枚举现有高风险入口：plugin install/delete/reload/exec、MCP create/update/delete/test、Webhook create/update/test、provider model config、报告 outputPath。
- 对每类入口只增加“批准后重试原操作”的显式按钮，不自动重放危险 shell。
- 对 approval payload 做 UI 可读摘要：action、target、cwd、风险原因、创建时间、审批状态。

验收标准：

- pending 复用原审批；rejected/cancelled/mismatch 直接 deny。
- e2e 至少覆盖一个非 Agent Run 的审批后安全重试。

### P3. 稳定化与提交拆分

目标：当前未提交增量已经很大，继续开发前需要开始拆分可审查提交，降低回滚风险。

建议拆分顺序：

1. Codebase Index 后端和测试：AST/FTS/vector/snapshot/citation/question/type evidence。
2. Agent Run 后端和测试：session/gate/archive/idea-auto-execute/approval-resume。
3. Governance Center / Runs tab / Codebase Center 前端。
4. e2e 和文档。

提交前规则：

- 不用 `git add .`，显式列文件。
- 每个提交都先跑对应定向测试，再至少跑 `npm run lint && npm test && git diff --check`。
- UI 提交必须跑 `npm run test:e2e`。
- 提交标题按中文动作式，例如 `Agent Run: 归档 Session Evidence 便于交接`。

### P4-P9. 中后期路线

- P4：把 Codebase Index 和 Agent Run evidence 做成可搜索历史知识库，仅限本地 SQLite，不做云同步。
- P5：把 Governance Center 从“聚合看板”升级成“工作队列”：待审批、待验证、待归档、待修复。
- P6：强化性能与资源控制：大文件扫描预算、FTS/vector cache 上限、长 e2e 服务清理、SQLite snapshot 保留策略。
- P7：补 i18n/accessibility 和低视觉噪声 UI polish，但不做营销页。
- P8：发布准备：迁移兼容、旧 SQLite schema 回归、打包体积、资源文件、Release Notes。
- P9：安全审计：路径越界、敏感文件读取、network upload、provider key 泄漏、approval bypass、artifact download allowlist。

## 禁止事项

- 禁止 `git reset --hard`、大范围 `git checkout --`、删除用户未确认的改动。
- 禁止 `git add .`；提交时显式列文件。
- 禁止删除治理、预算、审批、审计、委派、Agent Run、Codebase Index、Agent/Skill Registry。
- 禁止把 Paperclip/Crow5 学习成果回退成纯文档。
- 禁止引入云端多租户、企业账号、HR/雇佣 Agent 等 SaaS 形态。
- 禁止把审批批准理解为自动重放危险命令。危险终端命令仍需用户确认后重新执行。

## 提交与验证规则

每个功能独立提交。提交前至少运行：

```bash
npm test
npm run lint
git diff --check
```

如改 UI，还要运行：

```bash
npm run test:e2e
```

提交标题使用祈使/动作式中文，优先写成「模块或页面: 动作 + 目的」，例如：

```text
Codebase Index: 增加 FTS 查询排序
Agent Run: 展示审批与委派链路
侧边栏: 调整导航分组布局
```

## 可直接复制给下个 Codex 的启动提示

```text
请接手 Xike Lab 可视化面板项目。

项目路径：/Users/hxx/Desktop/00_项目/05_Claude可视化面板
当前分支：codex/paperclip-local-governance

先执行：
cd /Users/hxx/Desktop/00_项目/05_Claude可视化面板
pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short
test ! -e STOP
git log --oneline --decorate -8

然后阅读：
1. 任务交接.md
2. 上下文交接.md
3. Claude交接提示.md
4. docs/local-governance-learning-record.md
5. docs/README.md
6. docs/xikelab-agent-skill-registry.md
7. docs/crow5-deep-analysis-personal-growth-plan.md
8. docs/crow5-local-research-evidence-map.md

背景：这个分支已经完成 Paperclip 本地治理能力吸收，并继续完成 Crow5 技术精髓吸收的一批可运行功能。最新提交包括：
- 6010a23 侧边栏: 放大并分组功能入口
- a9e1a52 权限治理: 支持审批后安全重试
- ab7d007 文档: 更新 Crow5 吸收与本地治理交接
- 0d6f72f 权限治理: 统一判定高风险操作
- f027f3a Agent 工作台 UI: 展示 Runs 与 Codebase 查询
- 632ad95 Agent 运行时: 接入 Registry、Run 与代码索引后端
- f8b3c81 治理面板: 接入本地治理与委派自启动

当前工作区可能有 P0 Codebase Index per-file evidence cache、SQLite FTS/BM25、本地 hash-vector、TS/TSX/JSX AST、TS 类型级引用、TS assertion/satisfies/instantiation、method/member 语义引用、SQLite snapshot、citation chain，以及 P0 Agent Run governance lineage / Runs tab 治理筛选 / Governance Center / Approval Actions / 批准并续跑 / Preflight Review / approve-before-run diff gate / Session Evidence markdown UI 导出与文件归档 / patch quality / 执行后 archive artifact / `idea_to_archive` Run Draft / generated work manifest draft / 本地 Agent file-change plan / 可选模型 source patch manifest / execution completion / work plan / 受治理文件改动 / UI manifest editor / approval resume file change / expanded command policy / work evidence / artifact evidence / final archive / `idea-auto-execute` 安全自动验证执行器未提交增量。不要重新泛泛规划，不要删除预算、审批、审计、委派、Agent Run、Codebase Index、Agent/Skill Registry 等已落地功能，不要把 Xike Lab 改成 SaaS。先验证当前状态，再按用户最新任务继续。后续优先方向：P0 Codebase Index 二期深化，或 P0 Agent Run 会话化补强。
```
